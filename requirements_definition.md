# プロダクト要件定義書 — relay-lab

## 1. プロダクトビジョン

**「実機を配線する前に、実機と同じ端子番号で組んで確認できる」リレー回路シミュレーター。**

既存の回路シミュレーターは「リレー」という抽象部品を扱う。本プロダクトは `OMRON MY4N-D2 DC24V の端子 14 と端子 13` のように、現実に存在する部品を現実の端子番号で配線できることを最大の差別化要因とする。

## 2. ターゲットユーザーと課題

| ユーザー | 課題 |
|---|---|
| シーケンス制御の初学者・職業訓練生 | ラダー図は読めても、実際のリレーのどの端子に何を繋ぐか分からない。実機は高価で失敗が怖い |
| 制御盤設計・保全の実務者 | 現場作業前に配線を検証したい。頭の中の回路が正しいか手早く確認したい |
| 電気工事・シーケンス制御の指導者 | 実機を用意せずに端子レベルの配線を教材化したい |

共通する課題は、**回路図の理解と実配線の間にあるギャップ**。本プロダクトはそのギャップを埋める。

## 3. 主要機能

1. 実型番の部品ライブラリ（メーカー名・型番・端子番号・接点構成・コイル仕様・極性）
2. ドラッグ＆ドロップによる部品配置と、端子同士の配線
3. 電気的挙動のシミュレーション（導通・励磁・接点切替・点灯）
4. 状態収束計算による自己保持回路・インターロック回路の再現
5. コイル極性違反・電源短絡・未接続の検出と警告
6. 部品プロパティと端子情報の可視化
7. 回路の保存・読込（JSON / LocalStorage）

## 4. 成功の定義

- MVP: `requirements.md` に定義した MVP 第一目標と、検証回路テスト 1〜5 がすべて動作すること
- プロダクト: 実務者が「実機配線前の確認」に実際に使えると判断できること
- 拡張性: 新しいリレー型番の追加が、定義ファイル 1 枚の追加のみで完結すること

## 5. ユーザーストーリーと受け入れ条件

### US-1 実型番での配線
**初学者として、** 実際の型番と端子番号で配線したい。**なぜなら** 実機の端子に何を繋ぐかを覚えたいから。

- 部品パレットに `OMRON MY4N DC24V` のようにメーカー名と型番が表示される
- キャンバス上の部品に実端子番号（1〜14 など）が視覚的に表示される
- 端子にホバーすると「端子 14 / コイル + / DC24V」のような説明が出る

### US-2 動作確認
**学習者として、** 組んだ回路が実際に動くかを確認したい。

- ▶ シミュレーション開始で回路が評価される
- 押しボタンをマウスダウンで押下、マウスアップで復帰（モーメンタリ動作）
- リレーが励磁すると全接点が同時に切り替わる
- ランプが条件を満たすと発光表示される
- 配線が通電状態に応じて色分けされる

### US-3 自己保持回路の再現
**実務者として、** 自己保持回路とインターロックを再現したい。

- 押しボタンを一瞬押すだけでリレーが励磁し続ける
- 停止用 B 接点でリレーが消磁する
- B 接点によるインターロックで、片方が ON の間もう片方が ON にできない

### US-4 誤配線の検出
**学習者として、** 間違った配線に気づきたい。

- MY4N-D2 のコイル極性が逆なら励磁せず、警告が出る
- +24V と 0V が直結されていれば短絡警告が出る
- 未接続の端子が分かる

### US-5 回路の保存
**ユーザーとして、** 作った回路を残したい。

- 保存すると部品・位置・型番・配線・ビューポートが LocalStorage に残る
- 読込で復元される
- シミュレーション中の一時状態は保存対象外

## 6. 機能要件

### 6.1 対応部品（MVP）

| カテゴリ | 部品 |
|---|---|
| 電源 | DC24V 電源（+24V 端子 / 0V 端子） |
| スイッチ | モーメンタリ押しボタン A 接点 / B 接点 |
| リレー | OMRON MY2N DC24V / MY4N DC24V / MY4N-D2 DC24V |
| ランプ | DC24V 表示ランプ |
| ダイオード | 汎用ダイオード |
| 端子台 | 汎用端子台 |

### 6.2 画面構成

3 カラム（左：部品パレット / 中央：キャンバス / 右：プロパティパネル）＋ 操作バー（保存・読込・全消去・Undo・Redo・▶ 開始・■ 停止）。PC 利用を優先する。

### 6.3 キャンバス操作

パン / ズーム / 部品移動 / 複数選択 / 削除 / 配線 / Undo・Redo / **部品の左右反転**。部品移動時も配線は追従して切れない。

左右反転は端子の出る辺（左 ⇄ 右）を入れ替えるための操作で、電気的な意味は持たない。端子番号も配線も変わらない。電源やダイオードのように端子の向きが固定された部品を図面の右側へ置いたときに、配線が本体を横切らないようにするためのもの。

### 6.4 配線制約

接続は必ず「端子 → 端子」。部品そのものへの接続は不可。

### 6.5 部品パレット

カテゴリ分け（電源・スイッチ・リレー・ランプ・ダイオード・端子台）＋ 検索窓。MVP でも検索 UI は用意する。

## 7. 非機能要件

| 項目 | 要件 |
|---|---|
| プラットフォーム | デスクトップブラウザ優先。モバイル対応は範囲外 |
| バックエンド | 不要。クライアントサイド完結 |
| 永続化 | LocalStorage。将来 Supabase / Firebase へ移行可能な構造 |
| 性能 | シミュレーション 1 回の収束計算を 16ms 以内（部品 50 個規模） |
| 安全性 | 収束計算に最大反復回数（100）を設け、無限ループを防ぐ |
| 拡張性 | 部品追加はデータ駆動。エンジンに型番分岐を作らない |
| 保守性 | エンジンは React 非依存の純粋関数。単体テストで検証する |
| 言語 | UI・ドキュメントとも日本語 |

## 8. スコープ外（将来検討）

- 実体配線 / ラダー図の相互変換と自動生成
- AC100V / AC200V 回路、PLC、タイマーリレー、コンタクタ、ブレーカー、モーター
- 電圧・電流・消費電力の数値計算（負荷の直列接続を含む）
- 型番ごとの実機画像・ソケット図（著作権・ライセンスに配慮が必要）
- 逆接続による短絡・部品破損のシミュレーション
- メーカー横断の大規模部品ライブラリ（Panasonic / IDEC / 富士電機 / 三菱電機 / Schneider）
- クラウド保存・共有・ログイン
