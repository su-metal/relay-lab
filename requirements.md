# 作業単位の要求 — 選択した部品を揃える

> このファイルは作業単位ごとに上書き更新される。プロダクト全体の要件は `requirements_definition.md`、技術設計は `design.md` を参照。
>
> 前の作業単位（Step 0〜13）は完了済み。部品を配置し、実端子番号どうしを配線し、▶ で動作を確認でき、保存・書き出し・Undo・診断・部品検索・範囲選択・部品交換・操作ヘルプ・停止中の配線チェック・電流の向き・励磁経路の言語化・接点の図記号・タイマー（限時）・モバイル表示まで揃っている。

## 今回のスコープ

**Step 14 — 選択した部品を狙った基準で揃える。**

いまある「整列」（`▦ 配置を整列` / L キー）は**自動整理**で、グリッド吸着・近いものを寄せる・重なりをほどく、を一括で行う。`ALIGN_TOLERANCE`（32px）を超えて離れた部品は揃わないため、**「この 3 個だけ左端を合わせたい」「この列を等間隔にしたい」ができない。**

制御盤の図面は列を作って描くので、この操作が無いとマウスで 1 個ずつ寄せることになる。配線のレーン計算（`design.md` §8.7）は端子座標をそのまま拾うので、数 px のずれが経路の乱れとして図面に出る。

### 含むもの

- **揃える 6 種** — 左 / 左右中央 / 右 / 上 / 上下中央 / 下
- **均等 2 種** — 左右に均等 / 上下に均等（**部品の中心を等間隔に**）
- 操作バーの「揃える ▾」メニュー（8 種類を 1 ボタンに畳む）
- ヘルプへの追記

### 含まないもの

- **既存の自動整理（L キー）の変更** —— 役割が違うので混ぜない。重なりの解消は今のまま L の役目
- **ショートカットキーの追加** —— 無修飾キーは D / F / L / S で埋まっており、8 種類に充てられる残りが無い
- 部品の回転・整列ガイド（スナップライン）・等幅リサイズ
- 配線を対象にした整列（揃うのは部品の位置だけ）

## ユーザーストーリーと受け入れ条件

### US-W 狙った部品を狙った基準で揃えられる

**盤図を清書したいユーザーとして、** 選んだ部品の左端をきっちり合わせたい。

- 部品を 2 個以上選んで「揃える」→「左揃え」で、**いちばん左の部品は動かず**、他が左端に揃う
- 右・上・下も同じで、**基準にした端の部品は動かない**
- 「左右中央」は、幅の違う部品（電源 150px / MY4N 260px）でも**中心が 1 本の線に乗る**
- 選択していない部品は、たとえもっと左にあっても**基準にならず、動きもしない**
- 揃えた結果 部品が重なっても、勝手に下へ逃げない（重なりを解くのは L）

### US-X 列を等間隔に並べられる

**ラダー図として読ませたいユーザーとして、** 列のピッチを揃えたい。

- 3 個以上選んで「左右に均等」で、**両端の部品は動かず**、中心が等間隔に並ぶ
- 幅の違う部品が混ざっても、揃うのは**中心の間隔**
- 同じ操作を 2 回押しても結果が変わらない

### US-Y 押せないときは押せないと分かる

**初見のユーザーとして、** なぜ効かないのかを知りたい。

- 部品が 1 個以下のときは「揃える」ボタン自体が押せない
- 2 個のときは「均等」の 2 項目だけが押せず、`title` に「3 個以上」と出る
- どの操作も **Undo 1 回**で戻る
- メニューを開いたまま D を押しても、**選択が消えない**
- 狭い画面（900px 未満）でメニューが操作バーからはみ出さない

## 完了判定

- `npm test` が緑（`src/circuit/adapter/__tests__/align.test.ts` を含む）
- `npm run typecheck` が通る
- `design.md` §8.13 に設計判断が残っている
