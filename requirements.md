# 作業単位の要求 — 時間を扱えるようにする（タイマー）

> このファイルは作業単位ごとに上書き更新される。プロダクト全体の要件は `requirements_definition.md`、技術設計は `design.md` を参照。
>
> 前の作業単位（Step 0〜11）は完了済み。部品を配置し、実端子番号どうしを配線し、▶ で動作を確認でき、保存・書き出し・Undo・診断・部品検索・範囲選択・部品交換・操作ヘルプ・停止中の配線チェック・電流の向き・励磁経路の言語化・接点の図記号まで揃っている。

## 今回のスコープ

**Step 12 — シミュレーターに時間を入れる。**

自己保持もインターロックも組めるようになったが、実務のシーケンス制御は**限時（タイマー）抜きには組めない。** 「起動して n 秒後に次へ」「停止してもファンを n 秒回す」は自己保持と並ぶ基本要素であり、これが無いと組める練習回路がここで頭打ちになる。

タイマーはこれまで `requirements_definition.md` §8（スコープ外）に置かれていたので、**これはスコープの変更**でもある。

### 含むもの

- **オンディレイ（限時動作）** — 入力が入って設定時間後に接点が動く
- **オフディレイ（限時復帰）** — 入力と同時に動き、切れて設定時間後に戻る
- **実時間での動作** — ▶ 実行中に実際の秒数で進む。ただし `simulate()` は純粋関数のまま
- **インスタンスごとの設定時間** — プロパティパネルで秒指定（実機のダイヤルに相当）
- **計測中が読める表示** — ノードの残り時間、操作バーの経過時間

### 含まないもの

- **フリッカ（周期）動作・ワンショット** — 収束ループが「発振」（`design.md` §5.5）と区別できる必要があり、判定が一段複雑になる
- **実型番のタイマー**（OMRON H3Y-2 など）— 実端子番号には公式データシートの図の確認が要る（CLAUDE.md 設計原則 5）
- 一時停止・倍速・時刻の巻き戻し
- 接点のチャタリング、リレーの動作時間・復帰時間（`design.md` §6-4）
- 定格電圧の照合（`design.md` §6-3）

## ユーザーストーリーと受け入れ条件

### US-P 設定した時間だけ待ってから動く

**順序制御を学んでいるユーザーとして、** 「起動して n 秒後」を回路で組みたい。

- 押しボタン → オンディレイ → ランプ で、押してから設定時間後にランプが点く
- 設定時間の**手前では動かず、到達した瞬間に動く**
- 入力を切ると即座に戻る（限時なのは動作側だけ）
- 設定時間に届く前に入力を切ると**計り直しになる**（残り時間を持ち越さない）

### US-Q 切れてからも保持できる

**遅延停止を学んでいるユーザーとして、** 「止めても n 秒動かし続ける」を組みたい。

- オフディレイは入力と**同時に**動作する
- 入力を切ってから設定時間だけ保ち、到達した瞬間に戻る
- 保持中に再投入すると、**次に切ったときから計り直す**
- **▶ を押した直後、一度も入力していないオフディレイが動作していない**

### US-R 待っている最中が読める

**動かしているユーザーとして、** タイマーが計っているのか止まっているのかを知りたい。

- 計測中のノードに**残り時間**が出て、動作中とは別の見た目になる
- **計測中はコイルの配線が通電色**で、電流の向きも出る（接点はまだ動いていない）
- タイマーを置いた回路でだけ操作バーに経過時間が出る
- 計測しているタイマーが 1 個も無ければ再計算のループが止まる

### US-S 設定時間を部品ごとに決められる

**複数の遅延を組むユーザーとして、** 同じ型番を違う設定で並べたい。

- プロパティパネルで**秒**を指定できる（内部の ms を見せない）
- 同じ型番の T1・T2 に別々の設定時間を持たせられる
- 範囲外の値は上下限へ丸める（部品ごと捨てない）
- 設定変更は **Undo で戻せる**（回路の動きを変えるので履歴に載せる）
- 保存・読み込みで設定時間が保たれ、**旧データもそのまま読める**

### 完了判定

```
押しボタン → オンディレイ(2秒) → ランプ を組む
  → ▶ を押す → ボタンを押す → ノードが「残り 1.3秒」→「動作中」へ変わる
  → 2 秒後にランプが点く／離すと即座に消える
  → 設定を 0.5 秒へ変える → Undo で 2 秒へ戻る
  → オフディレイに差し替える → 押した瞬間に点き、離して 2 秒後に消える
  → タイマーを置かない回路では操作バーに秒数が出ない
  → npm test / npm run typecheck / npm run build がすべて通る
```

## 制約事項

- **`kind: "timer"` を作らない。** タイマーはリレーであり、`kind: "relay"` の `delay` の有無で分ける（CLAUDE.md 設計原則 7・`design.md` §5.13）
- **エンジンは時計を読まない。** `nowMs` は入力として受け取る（同 1）。テストは時刻を直接指定して書く
- **`energizedRelays` は接点であってコイルではない。** コイルを見たい場所では `coilEnergized()` を使う（同 8）
- **タイマーの出力を状態として保持しない。** `coilOn` と経過時間から導く（二重管理を作らない）
- **`requestAnimationFrame` で回し続けない。** カウント中のタイマーがあるあいだだけ解き直す
- **実端子番号を主張しない。** 汎用タイマーは `verified: false`（同 5）
- 記述・UI とも日本語
