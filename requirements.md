# 作業単位の要求 — 組んだ回路をラダー図で読む

> このファイルは作業単位ごとに上書き更新される。プロダクト全体の要件は `requirements_definition.md`、技術設計は `design.md` を参照。
>
> 前の作業単位（Step 0〜15）は完了済み。部品を配置し、実端子番号どうしを配線し、▶ で動作を確認でき、保存・書き出し・Undo・診断・部品検索・範囲選択・部品交換・操作ヘルプ・停止中の配線チェック・電流の向き・励磁経路の言語化・接点の図記号・タイマー（限時）・モバイル表示・部品の整列・経路確認モードまで揃っている。

## 今回のスコープ

**Step 16 — 組んだ実体配線を、ラダー図に変換して読めるようにする。**

キャンバスに描いてあるのは**実体配線図** —— `RY1 の 9` と `RY1 の 5` を電線で結んだ、という図 —— で、実端子番号を扱えることが本プロダクトの価値そのものだった。だが**その図は「この回路が何をする回路か」を答えない。** 線は端子の位置に従って引き回されており、条件と出力の並びは配線の形の中に埋もれている。

現場でその問いに答えるのはラダー図で、これは端子の位置を捨てて「左の母線から条件（接点）を通って出力（コイル）へ至る段」だけを並べたもの。**ターゲットユーザー（`requirements_definition.md` §2）はまさに「ラダー図は読めるが実機のどの端子に繋ぐか分からない」人**であり、読める側の図と組んだ配線を並べて照らせることが、この橋渡しをいちばん短くする。

### 含むもの

- **ラダー図への変換** —— 操作バーの `⊞ ラダー図` で開く。いまの配線から段を組み立てる（`adapter/ladder.ts`・純粋関数）
- **接点の直列・並列への縮約** —— 自己保持は「起動ボタンと保持接点の並列」として出る
- **実端子番号を図に残す** —— 接点の下に `9-5` を添える。ここを落とすと一般的なラダー図と変わらなくなる
- **出せない配線をそう言う** —— ブリッジ回路・母線に届いていない配線は理由を出して諦める
- ヘルプ（扱わないこと）への追記

### 含まないもの

- **ラダー図から実体配線への逆変換** —— ラダー図には接点が何番の端子かを書く場所が無く、実端子への割り当てが一意に決まらない（`requirements_definition.md` §8）
- **ラダー図の編集・書き出し** —— 図は配線から毎回組み直せる派生物で、保存対象にしない（CLAUDE.md 設計原則 9）
- **ラダー図の上での動作表示** —— 通電中の段を光らせる等。動きはキャンバス側の担当で、両方に出すと同じ規則が 2 箇所に散る
- **ショートカットキーの追加** —— 無修飾キーは D / F / L / S で埋まっている
- **ダイオードの表示** —— 還流ダイオードはコイルと並列に入る実装上の部品で、ラダー図の論理には現れない

## ユーザーストーリーと受け入れ条件

### US-AC 組んだ配線をラダー図として読める

**ラダー図は読めるが実配線に自信が無いユーザーとして、** 自分が組んだ配線が読める形で何になっているのかを見たい。

- `⊞ ラダー図` を押すと、負荷（コイル・ランプ）1 個につき 1 段のラダー図が出る
- 停止付き自己保持回路が、**起動ボタンと保持接点が並列**・停止ボタンが直列の 1 段になる
- 接点は a 接点 / b 接点の図記号で描かれ、**下に実端子番号**が付く
- 段は図面での位置の順（上から下）に並ぶ
- 部品が 1 個も無いときは押せない

### US-AD 実配線との食い違いを黙らない

**図面と実機を照らしているユーザーとして、** 図が実配線と違って見える箇所を知りたい。

- コイルの 0V 側にあった接点は出力の左へ移り、**移したことが断り書きに出る**
- 電源が 2 台以上あるときは、1 組の母線にまとめたことが断り書きに出る
- ダイオードは図に出さず、出していないことが断り書きに出る
- 母線に届いていない配線は、**どちら側に届いていないか**を段の位置に出す
- 直列と並列だけでは表せない配線（ブリッジ）は、そう言って諦める（近い形を出さない）

### US-AE 図が状態で変わらない

**動かしながら読むユーザーとして、** 図が押した瞬間に化けたら困る。

- 非励磁のリレーの b 接点は**今閉じていても b 接点**として出る（`conductingPairs()` を使わない）
- ▶ の実行中・経路確認中でも同じ図が出る
- ラダー図を開いても配線は変わらず、Undo の履歴も増えない

## 完了判定

- `npm test` が緑（`src/circuit/adapter/__tests__/ladder.test.ts` を含む）
- `npm run typecheck` が通る
- `design.md` §5.16・§8.15 に設計判断が残っている
