# 作業単位の要求 — 経路確認モードでスイッチを倒す

> このファイルは作業単位ごとに上書き更新される。プロダクト全体の要件は `requirements_definition.md`、技術設計は `design.md` を参照。
>
> 前の作業単位（Step 0〜15）は完了済み。部品を配置し、実端子番号どうしを配線し、▶ で動作を確認でき、保存・書き出し・Undo・診断・部品検索・範囲選択・部品交換・操作ヘルプ・停止中の配線チェック・電流の向き・励磁経路の言語化・接点の図記号・タイマー（限時）・モバイル表示・部品の整列・経路確認モードまで揃っている。

## 今回のスコープ

**Step 16 — 経路確認モードでスイッチを操作できるようにする。**

Step 15 の経路確認モードは**静止状態 1 枚**しか見せない。どのスイッチも倒していない状態の到達範囲だけなので、「S1 を入れたらどこまで電気が来るか」が読めない —— 実機を配線する前の確認はまさにその作業で、指で押しボタンをなぞりながら経路を追う。

`requirements_definition.md` §6.3 の時点では仮定操作を範囲外としていたので、**これはスコープの変更**でもある。ただし線は引く。

### 含むもの

- **経路確認中のスイッチ操作** —— 押しボタン（モーメンタリ）と切替スイッチ（オルタネート）の両方
- 倒した状態に**即座に追随する**到達範囲・止まっている箇所の一覧
- 予測であることが分かる操作子の見た目（破線・薄い塗り）
- 「リレーの接点は動かない」ことの明示
- ヘルプの更新

### 含まないもの

- **リレー・タイマーの接点を動かすこと** —— 収束ループが要り、実質「時間の進まない ▶」になる。動作した先は ▶ の領分
- **シミュレーションモードの変更** —— ▶ の挙動・排他の規則は今のまま
- **ショートカットキーの追加** —— 無修飾キーは D / F / L / S で埋まっている
- 倒した状態の保存（モードを出れば捨てる）

## ユーザーストーリーと受け入れ条件

### US-AC 倒した先の経路が読める

**実機を配線する前に確認したいユーザーとして、** スイッチを入れた状態の経路も動かさずに見たい。

- 経路確認中、スイッチに操作子が出る（停止中は出ない）
- 押しボタンは**押している間だけ**、切替スイッチは**もう一度押すまで**倒れたままになる
- 倒すと、その先の線が到達色・通電色に変わる
- 倒したスイッチは「止まっている箇所」の一覧から消える
- b 接点を倒すと逆に開き、そこが新しい止まり所として一覧に出る
- 一覧の文言が操作状態に合う（倒していない a 接点は「操作すると閉じます」、倒した b 接点は「操作をやめると閉じます」）

### US-AD 動いていないことを取り違えない

**初学者として、** 見ているものが「実行結果」ではないと分かりたい。

- **リレーの接点は動かない** —— コイルが励磁色になっても、そのリレーの接点の先は非通電のまま
- そのことが一覧の注記に、注記の中でいちばん強い字で書いてある
- 操作子は破線で、倒しても実行中のようなべた塗りにはならない
- ▶ を押すとモードを抜け、そこからは接点も動く

### US-AE モードの出入りで状態が残らない

**行き来するユーザーとして、** 前に倒した状態が見えないまま効いていては困る。

- モードに入るとき・出るとき、どちらでも倒した状態は捨てられる
- 入り直すとすべて OFF から始まる
- 停止中（どちらのモードでもない）はスイッチを倒せない
- ▶ を押すと経路確認から抜け、押下状態も捨てられる

## 完了判定

- `npm test` が緑（`preview.test.ts` / `path-preview.test.ts` / `simulationStore.test.ts` の追加分を含む）
- `npm run typecheck` と `npm run build` が通る
- `design.md` §5.15「スイッチは倒せる。リレーは動かない」と §8.14 に設計判断が残っている
