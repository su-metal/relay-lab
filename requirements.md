# 作業単位の要求 — モバイル端末での表示を最適化する

> このファイルは作業単位ごとに上書き更新される。プロダクト全体の要件は `requirements_definition.md`、技術設計は `design.md` を参照。
>
> 前の作業単位（Step 0〜12）は完了済み。部品を配置し、実端子番号どうしを配線し、▶ で動作を確認でき、保存・書き出し・Undo・診断・部品検索・範囲選択・部品交換・操作ヘルプ・停止中の配線チェック・電流の向き・励磁経路の言語化・接点の図記号・タイマー（限時）まで揃っている。

## 今回のスコープ

**Step 13 — 携帯とタブレットで使えるようにする。**

画面は 3 カラム（左 240px / 右 280px）固定で、**携帯の幅ではキャンバスがほとんど残らない。** 操作バーは横に溢れ、パレットの D&D は指では動かず（HTML5 の DnD はマウス専用）、素の 1 本指ドラッグは範囲選択に取られているので図面を動かすこともできない。実質、モバイルでは開けるだけで何もできない状態にある。

`requirements_definition.md` §7 でモバイルは「範囲外」としていたので、**これはスコープの変更**でもある。

### 含むもの

- **狭い画面のレイアウト** — 3 カラムを畳み、キャンバス 1 枚＋下から出すシート（部品 / プロパティ / 診断）
- **タッチ操作** — 1 本指で画面移動、2 本指で拡大・縮小、パレットのタップで部品を置く
- **指で狙える当たり判定** — 端子・押しボタン・操作バー・ズームボタン
- **モバイルの画面まわり** — viewport、pull-to-refresh の抑止、入力欄の拡大防止、セーフエリア
- **ヘルプへのタッチ操作の追加**

### 含まないもの

- **デスクトップの見た目・操作の変更** —— 3 カラムも、範囲選択も、キー割り当ても今のまま
- **モバイル専用の画面・パネル** —— 中身は 3 カラムと同じコンポーネントを入れる（2 つに分けると片方だけ直す事故が起きる）
- 指でのつなぎ替え（配線の端を掴む）の作り込み、長押しメニュー、部品の回転
- ネイティブアプリ化・オフライン（PWA）
- 端子番号の表示そのものの作り替え（拡大は端末のピンチに任せる）

## ユーザーストーリーと受け入れ条件

### US-T 携帯の画面でも図面が読める

**移動中に確認したいユーザーとして、** 携帯でもキャンバスを広く使いたい。

- 幅 900px を切ると 3 カラムが畳まれ、キャンバスが画面いっぱいになる
- パレット・プロパティ・診断は画面下のタブから開き、**キャンバスを押し上げず上に重なる**
- 閉じている間も、選択中の部品数と指摘の件数がタブの数字で分かる
- 窓を広げれば 3 カラムに戻り、シートは残らない

### US-U 指だけで回路を組める

**タブレットで教えているユーザーとして、** マウスなしで部品を置いて配線したい。

- パレットの部品を**タップすると、いま見えている範囲の中央に置かれる**
- 続けて置いても重ならず、置いた部品は必ず画面の中に見えている
- 端子どうしを指でドラッグして配線できる
- 1 本指のドラッグで画面が動き、2 本指で拡大・縮小できる
- 押しボタンを指で押している間だけ接点が閉じる

### US-V モバイルでも操作が失われない

**キーボードを持たないユーザーとして、** PC と同じことができてほしい。

- Delete キーが無くても操作バーの「削除」で消せる
- 書き出し・読み込み・整列・全体表示・Undo / Redo が**折り返して全部見えている**（横スクロールに隠れない）
- 保存できない環境では狭い画面でもその表示が出る
- ヘルプにタッチ操作の一覧がある

### US-W 触っても画面が壊れない

**スマートフォンのユーザーとして、** 図面を触っているのに端末側の動作に取られたくない。

- キャンバスを下へ払ってもページが再読み込みされない
- 部品名やタイマーの秒数を打つときにページが勝手に拡大しない
- 端子番号を読むためのピンチ拡大は禁止されていない
- 画面下のタブがホームインジケーターに重ならない

### 完了判定

```
携帯の幅（390×844・タッチ）で開く
  → 3 カラムが畳まれ、下に「部品 / プロパティ / 診断」のタブが出る
  → 「部品」→ MY4N-D2 をタップ → 画面の中央にリレーが置かれる
  → もう 1 個置く → 重ならず、置いた部品が画面に見えている
  → 1 本指で図面を動かせる／2 本指で拡大できる
  → 「診断」タブに指摘の件数が出る
  → 幅 1440px で開くと 3 カラムのまま（デスクトップは変わらない）
  → npm test / npm run typecheck / npm run build がすべて通る
```

## 制約事項

- **判定の軸を 1 つに畳まない。** 狭さ（`max-width`）と指（`pointer: coarse`）は別（`design.md` §8.12）
- **ブレークポイントの数値を CSS と TypeScript の 2 箇所に持たない。** 狭いときの見た目は `data-compact` 属性で切り替える
- **座標の計算を React コンポーネントに書かない。** タップ配置と画面寄せは純粋関数（`place-component.ts`）に置き、Vitest で検証する（CLAUDE.md 設計原則 1 と同じ扱い）
- **端子の点そのものを大きくしない。** 広げるのは当たり判定だけ（実端子番号が読めなくなる）
- **ノードの `visual` を変えない。** 部品の寸法は定義データで、モバイルの都合で動かさない
- 記述・UI とも日本語
