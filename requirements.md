# 作業単位の要求 — 調光操作卓から VP・スクリーン・モニターを実際に動かす

> このファイルは作業単位ごとに上書き更新される。プロダクト全体の要件は `requirements_definition.md`、技術設計は `design.md` を参照。
>
> 前の作業単位（Step 0〜25）は完了済み。直前の Step 25 で、プロジェクター（VP）・VP コントローラー・モニター・電動スクリーンを部品として追加し、調光操作卓に VP電源・スクリーン上昇/停止/下降の 4 操作子を足した。ただし操作子は通信で「送られる」ところまでで、コントローラ側の端子 33〜36 はまだ何も駆動しない「端子として出ているだけ」の状態だった。

## 背景 — 通信は繋がったが、コントローラの出力接点がまだ無い

調光コントローラ（`dimming-controller-16ch`）の ON/OFF 出力（端子 24〜39・オープンコレクタ）は、Step 21 で「端子として出すが、まだ接点としては働かない」と決めたまま、Step 24・25 でも扱わずに残していた。VP電源・スクリーン上昇/停止/下降のボタンを倒しても、通信線の先の端子 33〜36 は導通判定に一切参加せず、その先に置いた VP コントローラー・電動スクリーンを実際に動かす配線を組むことができなかった。

`analog-source`（調光出力を持つ機器の kind）に接点を持たせる構造判断が要る、というのが Step 21・24・25 で繰り返し「次スコープ」に残していた課題そのもの。

## 今回のスコープ

**Step 26 — オープンコレクタ出力（`digitalOutputs`）をエンジンに追加し、調光操作卓から VP・スクリーン・モニターまでを実際に配線して動かせるようにする。**

### 含むもの

- **エンジンの新機能：オープンコレクタ出力（`AnalogSourceDefinition.digitalOutputs`）** — 通信で受けた値（%）が 0 より大きいときだけ、信号端子とコモン端子を実際に導通させる（design.md §5.22）。0–10V の電圧を出す `channels` とは別の面に持たせ、「信号端子とコモンを union しない」原則を破らない。`normallyClosed` で NO/NC の対を表せる
- **`dimming-controller-16ch` の端子 33・34・35・36 を `digitalOutputs` として実装** — VP電源・スクリーン上昇/停止/下降の 4 信号（`communication.receives` に追加）が、実際にこれらの端子を GND（21）へ落とす接点として動く
- **調光操作卓 → コントローラ → VP コントローラー・電動スクリーンが一気通貫で動くことをテストで確認**（`av-equipment.test.ts`）
- **モニターは操作卓の実在する電源接点（無電圧接点 4-5-6・オープンコレクタ出力 2-3-9）で外部リレーを介して連動させる** — 社内仕様書（ver.1.1）のコンソール前面パネルにモニター専用ボタンの記載が無いため、新しい操作子は作らない（ユーザー判断）
- **モニター・電動スクリーンの見た目**（`AvBody.tsx`）— モニターは点灯すると画面にプレースホルダーの絵（実際の画像アップロードは扱わない・ユーザー判断）、電動スクリーンは上昇/下降中に生地が流れるアニメーションと上昇/停止/下降の独立したインジケーターを出す。どちらも実際の状態（高さ・映像）は保存も計算もしない（design.md §6）

### 含まないもの

- **端子 32（電源 ON/OFF）・37（AUX2）・38/39（電源制御 NO/NC）を接点として駆動すること。** 対応する操作卓側の送信元（前面パネルの割り当て）がまだ無く、引き続き次スコープに残す
- **モニターの映像アップロード。** 「表示中かどうか」だけをプレースホルダーの絵で示す（ユーザー判断）
- **電動スクリーンの上昇・下降・停止のインターロック。** Step 25 から変更なし（design.md §6）
- **シリアル通信（RS-232C）の中身。** Step 25 から変更なし

## ユーザーストーリーと受け入れ条件

### US-AZ 調光操作卓の VP・スクリーンのボタンで実機が実際に動く

**盤を再現するユーザーとして、** 調光操作卓の VP電源・スクリーン上昇/停止/下降ボタンで、その先に置いた VP コントローラー・電動スクリーンを実際に動かしたい。

- VP コントローラー・電動スクリーンを調光コントローラの端子 33・34・35・36 へ配線し、調光操作卓と調光コントローラを通信線で繋ぐと、操作卓のボタンで実機が反応する
- VP電源ボタンを倒すと（プロジェクターの USB 給電があれば）VP コントローラーのコイルが励磁する
- スクリーン上昇/停止/下降ボタンを倒すと、対応する補助コイルだけが励磁する。複数を同時に倒しても互いに干渉しない
- ボタンを倒していなければ、いずれの出力も GND へ落ちない（浮いたまま）

### US-BA 調光操作卓の電源ボタンでモニターが連動する

**盤を再現するユーザーとして、** 調光操作卓の電源ボタンでモニターの電源も連動させたい。

- 調光操作卓の電源接点（端子 2 のオープンコレクタ出力・端子 9 の GND）を外部リレーのコイル駆動源にし、リレーの接点でモニターの AC100V ラインを断続する配線を組める
- 操作卓の電源ボタンを倒すとリレーが動作し、モニターが点灯する。倒さなければ点灯しない

### US-BB モニター・電動スクリーンが視覚的に分かる

**盤を再現するユーザーとして、** モニター・電動スクリーンの状態を見た目で確認したい。

- モニターは点灯すると画面にプレースホルダーの絵が出て「表示中」と表示され、消灯すると暗い画面に戻る
- 電動スクリーンは上昇/下降中は生地に流れるアニメーションが出て方向が分かり、停止すると止まる。上昇・停止・下降それぞれのインジケーターが独立して点灯する
