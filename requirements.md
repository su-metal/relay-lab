# 作業単位の要求 — 動かす前に励磁する経路を読む

> このファイルは作業単位ごとに上書き更新される。プロダクト全体の要件は `requirements_definition.md`、技術設計は `design.md` を参照。
>
> 前の作業単位（Step 0〜14）は完了済み。部品を配置し、実端子番号どうしを配線し、▶ で動作を確認でき、保存・書き出し・Undo・診断・部品検索・範囲選択・部品交換・操作ヘルプ・停止中の配線チェック・電流の向き・励磁経路の言語化・接点の図記号・タイマー（限時）・モバイル表示・部品の整列まで揃っている。

## 今回のスコープ

**Step 15 — 動かす前に、電源から電位が届いている範囲を読めるようにする。**

停止中に付いている色は**役割配色**（`design.md` §5.8）で、「この線はいつか電源につながるか」を答えている。接点やスイッチの先はまとめて制御線＝黄になるため、**「いま実際にどこまで来ていて、どこで止まっているか」が読めない。**

実機を配線する前に確認する、というこのプロダクトの目的からすると、経路が読めるのが ▶ を押した後だけなのはひとつ遅い。未接続も短絡もダイオードの逆挿しも通電前から配線図の上で決まっているのと同じで、**電位がどこまで届くかも決まっている。**

### 含むもの

- **経路確認モード** —— 操作バーの `⚡ 経路確認` で入る。静止状態（どのスイッチも操作していない・どのリレーも励磁していない）の到達範囲をキャンバスに塗る
- **止まっている箇所の一覧** —— 右カラムに、電位が止まっている接点を**実端子番号**で出す
- 実行中の色との描き分け（破線・発光なし）と、凡例の 3 通り目
- ヘルプへの追記

### 含まないもの

- **仮定操作（「このスイッチを押したら」）** —— 収束ループが要り、実質「時間が進まない ▶」になる。押した後は ▶ の領分
- **既存の役割配色（§5.8）の変更** —— 問いが違うので混ぜない。排他で切り替える
- **ショートカットキーの追加** —— 無修飾キーは D / F / L / S で埋まっている
- 部品そのものの状態表示（励磁・点灯）—— 動いていないので出さない

## ユーザーストーリーと受け入れ条件

### US-Z 動かさずに電位の届く範囲が読める

**実機を配線する前に確認したいユーザーとして、** 電源を入れなくても経路を追いたい。

- `⚡ 経路確認` を押すと、電源 + 側から届いている線と 0V 側から届いている線が**それぞれの色の破線**になる
- 押しボタンや A 接点の先の線は、**まだ届いていない**線として薄いまま残る
- 電源に直結したコイル・ランプの経路は**通電色**になる（この状態のまま励磁する）
- 実行中の緑と違い**光らない** —— 「今流れている」と「電源を入れればこうなる」が見た目で分かれる
- 部品そのものは励磁・点灯を表示しない（動いていないため）

### US-AA どこで止まっているかが端子番号で分かる

**配線の抜けを探しているユーザーとして、** 経路がどこで途切れたのかを知りたい。

- 電位が止まっている部品が**キャンバスで囲まれる**
- 右カラムの一覧に `+ 側 | RY1 9 → 5 | このリレーが動作すると閉じます` の形で並ぶ
- 一覧の項目を押すとその部品が選択される
- 閉じれば短絡になる接点は**一覧に出ない**（それは配線チェックの担当）
- 静止状態のまま励磁・点灯する負荷があれば、その件数が出る

### US-AB ▶ と混ざらない

**初見のユーザーとして、** 今どちらを見ているのかを間違えたくない。

- 経路確認中に ▶ を押すとモードから出て、実行の色に切り替わる
- 実行中に `⚡ 経路確認` を押すと実行が止まり、押下状態と結果は捨てられる
- 両方の色が同時に載ることはない
- 凡例が、停止中・経路確認中・実行中の 3 通りで切り替わる
- 部品が 1 個も無いときは `⚡ 経路確認` が押せない

## 完了判定

- `npm test` が緑（`src/circuit/engine/__tests__/preview.test.ts` と `src/circuit/adapter/__tests__/path-preview.test.ts` を含む）
- `npm run typecheck` が通る
- `design.md` §5.15・§8.14 に設計判断が残っている
