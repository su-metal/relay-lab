# 作業単位の要求 — 編集操作の仕上げ（範囲選択）

> このファイルは作業単位ごとに上書き更新される。プロダクト全体の要件は `requirements_definition.md`、技術設計は `design.md` を参照。
>
> 前の作業単位（Step 0〜7「MVP 完成」）は完了済み。部品を配置し、実端子番号どうしを配線し、▶ で動作を確認でき、保存・Undo・診断・部品検索まで画面上で動く。その後の小さな追加として、D キー単独での削除と部品の左右反転（F キー）が入っている。

## 今回のスコープ

**Step 8 — 範囲選択で部品と配線をまとめて編集する。**

図面を直すときの単位は「1 個」ではなく「このあたり一帯」になる。囲んで消す操作が無いと、部品と配線を 1 個ずつクリックして消すことになり、Undo もその回数だけ必要になる。

### 含むもの

- **枠で囲む範囲選択。** 何もない所を左ドラッグすればそのまま枠が出る（**画面移動は Shift+ドラッグ**・中／右ドラッグ・ホイール）
- **配線そのものを枠で選べる。** 両端の部品を含まない枠でも、枠を横切る配線が選ばれる
- **選択対象の切り替え。** 部品＋配線 / 部品のみ / 配線のみ を操作バーから選ぶ
- **まとめて削除しても履歴は 1 手。** 部品 3 個と配線 2 本を一度に消したら、Undo 1 回で全部戻る

### 含まないもの

- 範囲選択した部品の一括移動・整列・コピー / 貼り付け
- 選択範囲の保存（グループ化）
- 電圧・電流の数値計算、ダイオードの整流作用、タイマー（`design.md` §6）

## ユーザーストーリーと受け入れ条件

### US-G 囲んでまとめて消せる

**ユーザーとして、** 描き直したい一帯をまとめて消したい。

- 枠で囲むと、**枠にすっぽり収まった部品**と**枠に触れた配線**が選ばれる
- かすっただけの部品は選ばれない（囲んだつもりの無い MY4N が一緒に消えない）
- 選択したものを Delete / Backspace / D / 「選択を削除」で消せる
- **消した後、Undo 1 回で全部戻る**（要素の数だけ Undo を押さない）

### US-H 配線だけを引き直せる

**ユーザーとして、** 部品は残したまま、間違えた配線だけを消したい。

- 電源とリレーを結ぶ長い 1 本を、**途中で小さく囲むだけで**選んで消せる（両端の部品は選ばれない）
- 「配線のみ」に切り替えると、部品を含む枠で囲んでも配線だけが選ばれる
- 「部品のみ」に切り替えると、配線を横切る枠でも部品だけが選ばれる
- 対象の切り替えは**範囲選択にだけ効く。** 単体クリックはどのモードでも部品・配線の両方を選べる（プロパティパネルが使えなくならない）

### 完了判定

```
枠で部品を囲む → 部品とその配線が選ばれる
  → 部品を含まない枠で配線の途中を囲む → その配線だけが選ばれる
  → 対象を「配線のみ」「部品のみ」に切り替えて、それぞれ片方だけが選ばれる
  → 「配線のみ」のまま部品をクリックすると選択できる（React Flow との食い違いが残らない）
  → まとめて削除 → Undo 1 回で全部戻る
```

## 制約事項

- **当たり判定は `circuit/adapter/selection.ts` の純粋関数に閉じる**（`design.md` §8.6）。React コンポーネントや React Flow の内部状態に判定を書かない
- **React Flow の選択変更を握り潰さない。** React Flow は選択変更時に内部の `selected` を同時に書き換えるため、変更を捨てると食い違いが残り、その部品が以後クリックで選べなくなる。枠が出ている間は**選択集合を毎フレーム宣言し直す**（`design.md` §8.6）
- 削除の入口は `removeElements(componentIds, connectionIds)` の 1 本だけ。要素ごとに消す API を残さない（`design.md` §7）
- 範囲選択の設定（モード・対象）は画面の操作モードであり、`circuitStore`（保存対象＋履歴）に混ぜない
- 記述・UI とも日本語
