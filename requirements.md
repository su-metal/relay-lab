# 作業単位の要求 — 初心者が「経路」を読めるようにする

> このファイルは作業単位ごとに上書き更新される。プロダクト全体の要件は `requirements_definition.md`、技術設計は `design.md` を参照。
>
> 前の作業単位（Step 0〜10）は完了済み。部品を配置し、実端子番号どうしを配線し、▶ で動作を確認でき、保存・書き出し・Undo・診断・部品検索・範囲選択・部品交換・操作ヘルプ・停止中の配線チェック・CI まで揃っている。

## 今回のスコープ

**Step 11 — 「動く」から「なぜ動くのかが分かる」へ。**

回路は正しく解けており、色も出ている。だが**色は状態しか言わない。** 初めてリレー回路を読む人の問いは「どちらへ流れているのか」「何を通って励磁しているのか」「なぜ動かないのか」の 3 つで、どれも今の画面は答えていない。ノードの絵も同じで、リレーの本体である「COM が NC から NO へ移る」という動きが図として存在しない。

### 含むもの

- **電流の向き。** 通電中の線に、電流の流れる向きを動きで示す（案1）
- **励磁経路の言語化。** 選択した負荷が「何を通って」通電しているのかを実端子番号で並べる（案2）
- **通電しない理由。** 非通電の負荷について、どちら側の電源が届いていないかと、手前で開いている接点を挙げる（案3）
- **接点の図記号。** リレーのノードに、COM がどちらへ倒れているかを端子番号つきで描く（案4）

### 含まないもの

- 収束の反復を追うステップ再生（`design.md` §6-4 の「時間の概念がない」と衝突しないための言い回しが別途要る）
- ラダー図（シーケンス図）の併記
- 経路の全列挙（並列がある回路では指数になる。橋＝必ず通る線までに留める）
- 電圧・電流の数値（`design.md` §6-3）

## ユーザーストーリーと受け入れ条件

### US-L 電流がどちらへ流れているか分かる

**回路を初めて読むユーザーとして、** 緑の線を見て「どちらからどちらへ」を知りたい。

- 通電中の線に、電流の向きが**動き**で出る（色の軸は §5.6 が使い切っているので足さない）
- **配線をどちら向きに引いたかで向きが変わらない。** 同じ回路を逆向きに配線しても向きは同じ
- **極性なしのコイルを逆接した回路でも正しい。** 13 番が入口になる
- **並列に分かれた区間には向きを出さない。** 実際に分流するので 1 本に決まらない
- 自己保持の紫の破線も、流れる向きが電流の向きと一致する
- 凡例に「電流の向き」が出る

### US-M 何を通って励磁しているのかが読める

**自己保持回路を学んでいるユーザーとして、** コイルが何によって保持されているのかを言葉で知りたい。

- リレー／ランプを選ぶと、`電源 + → …実端子番号… → 負荷 → … → 0V` の経路が並ぶ
- **自己保持中は、そのリレー自身の接点（9 → 5）が経路に現れる**
- 並列に分かれていて一本道に絞れないときは、そう明示する（黙って繋げない）
- 停止中は何も出さない（消磁しているのか動いていないのかを取り違えさせない）

### US-N なぜ動かないのかが分かる

**配線したのに動かないユーザーとして、** どこで止まっているのかを知りたい。

- 非通電の負荷について、**両端それぞれが + / 0V に届いているか**が出る
- **閉じれば電源に届く接点だけ**を、閉じる条件つきで挙げる（開いている接点を全部並べない）
- インターロックで開いた b 接点は「相手が非励磁に戻ると閉じます」と出る
- 配線が無いだけの負荷に、接点の指摘を出さない

### US-O リレーが何をする部品か絵で分かる

**リレーを初めて触るユーザーとして、** 中で何が動いているのかを見たい。

- ノードに接点の図記号が出て、COM の可動片が閉じている側へ倒れている
- **図記号に実端子番号が添えられ、外周の端子と結び付く**
- a 接点のみのリレー（G7L）に b 接点を描かない
- 停止中は静止状態の絵になる

### 完了判定

```
▶ を押す → 起動ボタンを押す → 緑の線に流れる向きが出る
  → RY1 を選ぶ → 「励磁している経路」に PS1 +24V → S2 1→2 → コイル 14→13 → PS1 0V が並ぶ
  → ボタンを離す → 経路に RY1 9→5 が現れる（自己保持へ移ったことが言葉で読める）
  → ボタンを押す前に RY1 を選ぶ → 「14 に + 側が届いていません／S1 を押すと閉じます」が出る
  → リレーのノードで、COM の可動片が 1 番側から 5 番側へ倒れる
  → npm test / npm run typecheck / npm run build がすべて通る
```

## 制約事項

- **経路グラフを 3 箇所で組み直さない。** `adapter/path-graph.ts` に 1 つ置き、自己保持・電流の向き・経路説明が共有する（`design.md` §5.9〜§5.11）
- **判定はすべて adapter の純粋関数に置く。** React コンポーネントに経路探索を書かない（CLAUDE.md 設計原則 1）
- **接点の開閉規則をエンジンの外に写さない。** 今どれが閉じているかは `conductingPairs()` / `closedContactPairs()` に聞く
- **誤った向き・誤った経路を出すくらいなら出さない。** 並列区間は向きも一本道も申告しない（§5.9 の紫と同じ判断）
- **型番分岐を書かない**（CLAUDE.md 設計原則 2）。接点の形は `ncTerminal` の有無から従う（同 6）
- 記述・UI とも日本語
