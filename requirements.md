# 作業単位の要求 — 調光と接点を橋渡しする

> このファイルは作業単位ごとに上書き更新される。プロダクト全体の要件は `requirements_definition.md`、技術設計は `design.md` を参照。
>
> 前の作業単位（Step 0〜21）は完了済み。Step 20 で 0–10V のアナログ量が入り、Step 21 で実機の調光コントローラと位相制御調光器が置けるようになった。

## 背景 — 端子は出したが、まだ働いていない

Step 21 で実機の機器を置けるようにしたが、**接点まわりの端子は「出しただけ」**で止めてある。

| 端子 | 現状 |
|---|---|
| ライトコントローラの **カットリレー** | 機器ごと未実装 |
| 調光コントローラの **ON/OFF 出力 24–39**（オープンコレクタ） | 端子はあるが働かない |
| 調光操作卓の **無電圧接点 4-5-6** | 機器ごと未実装 |

どれも **アナログ量と接点の橋渡し**で、いまのエンジンに無い機構が要る。ここが繋がると、盤の「調光した結果でリレーが動く」「操作卓のボタンで接点が出る」が再現できる。

## 今回のスコープ

**Step 22 — 接点の駆動源を、コイル以外にも広げる。**

### 中心になる考え方

**接点はコイルだけで動くものではない。** 実機には次の 3 通りがある。

| 駆動源 | 例 |
|---|---|
| コイル | 普通のリレー・電磁接触器・タイマー |
| **アナログ量**（明るさが閾値を跨ぐ） | カットリレー |
| **人の操作** | 操作卓のボタン、それに連動するオープンコレクタ出力 |

`RelayDefinition` は接点・端子・図記号・経路説明・ラダー図のすべてを既に持っているので、**駆動源だけを広げれば残りは全部そのまま効く。** これは `ncTerminal` / `noTerminal` を省略可能にして接点の形を広げたのと同じ拡張の仕方（`design.md` §3.2）。

**`coil` を省略可能にする。** カットリレーにも操作卓のボタンにもコイルは無い。実機に無いコイル端子を作って埋めるのは、`ncTerminal` を空文字で埋めるのと同じ誤り（CLAUDE.md 設計原則 6）。

### アナログが接点を動かすと、第 2 パスの前提が変わる

`design.md` §5.17 は「**アナログ量は接点を動かさない**ので収束ループの中には入れない」と書いている。**カットリレーはこれを崩す。** 明るさが接点を動かし、その接点がネットの形を変え、ネットの形が明るさを決める —— コイルと接点の相互依存とまったく同じ構造になる。

だから**収束ループの中でアナログを解く**。混ぜるのではなく、反復のたびに「ネットを組む → コイルとアナログを評価 → 接点を決める」を回す。§5.17 の記述は同じ作業の中で直す。

**それでも導通レイヤに電圧は混ぜない**（CLAUDE.md 設計原則 9）。`NetState` は今までどおり「どの電源の + / 0V に届くか」だけを持ち、電圧値は別に重ねたままにする。変わるのは**解く順番**であって、層の分け方ではない。

### 含むもの

- `RelayContact` の駆動源を広げる（`trigger` … アナログ閾値 / `operationId` … 人の操作）
- `RelayDefinition.coil` を省略可能にし、`operations` と `analogInputs` を足す
- 接点ごとの動作状態（`operatedContacts`）—— 1 台の機器の中で接点ごとに駆動源が違う
- 収束ループにアナログを取り込む
- **ライトコントローラ**（4 回路・0–10V 入力・カットリレー 4 個・動作点は回路ごとに調整可）
- **調光操作卓**（15 端子・無電圧接点・オープンコレクタ出力）
- 操作できる機器の状態を画面から倒せるようにする（実機のボタン）

### 含まないもの

- **調光コントローラの ON/OFF 出力 24–39 を繋ぐこと** —— 16 回路それぞれの駆動源（シーン記憶・フェーダー割付）は操作卓の内部ロジックで、機器 1 台では決まらない。端子は出したまま
- **PWM 出力の波形**（1kHz / 300Hz）・位相制御の波形
- **シーン記憶・フェーダー操作・パラメータ設定**
- 通信（RS-485 / DMX）、フォトカプラ入力
- 定格容量（500mA / 20mA / 800W）の超過判定

### 操作の状態は保存しない

人が倒した状態は `SimulationInput` で受け、`CircuitDocument` には持たない。**オルタネートスイッチと同じ扱い**で、■ で停止すると OFF 位置へ戻る（`design.md` §4.7）。盤の状態は配線ではないので、保存対象にすると「保存した回路を開くと勝手に照明が点いている」ことになる。

## ユーザーストーリーと受け入れ条件

### US-AR カットリレーが明るさで動く

**盤を再現するユーザーとして、** 調光を絞ったら連動してリレーが落ちる動きを見たい。

- 0–10V 入力を繋ぐと、その明るさに応じてカットリレーの接点が開閉する
- **動作点（0〜50%）を回路ごとに設定できる**（実機の CUT ADJ.）
- 明るさが動作点を下回ると動作し、上回ると復帰する
- 4 回路が独立して効く
- **コイルが無いので、コイルの極性も未接続も出ない**

### US-AS 操作卓のボタンで接点が出る

**盤を図にするユーザーとして、** 操作卓の電源ボタンに連動する接点を配線したい。

- 無電圧接点（4-5-6）が c 接点として働き、COM が NC / NO のどちらかへ倒れる
- オープンコレクタ出力（2・3）が GND へ落ちる形で働く
- **画面からボタンを倒せる**（実機の操作に相当）
- ■ で停止すると OFF 位置へ戻る

### US-AT コイルの無い機器が破綻しない

**既存の機能を使うユーザーとして、** 機器が増えても診断や図が壊れてほしくない。

- コイルが無い機器で**コイルの極性違反・コイルの未接続が出ない**
- 自己保持の検出・経路説明・ラダー図・接点の図記号が、コイルの有無に関わらず動く
- 既存のリレー・接触器・タイマーの動きが 1 つも変わらない

## 完了判定

- `npm test` が緑
- US-AR〜AT が動く
- `design.md` §3.2・§4・§5.5・§5.17 と `requirements_definition.md` §6.1 が更新されている
- **§5.17 の「アナログ量は接点を動かさない」を同じ作業の中で書き換える** —— 書いた本人が前提を崩したのに古い記述を残すと、次に読む人が逆の判断をする

---

## 引き継ぎメモ（リポジトリの外の決定事項）

- 実機の仕様書は社内資料。**型番・製造元・製品名はリポジトリに書かない。** 端子番号と挙動だけを実機に合わせる
- 調光の逆特性（0V=100%）は仕様書に明記あり。ただし実機は極性切替式なので、インスタンスの `dimmerSettings` が持つ
- ログイン：Supabase Auth + RLS で確定。未決は「公開プロダクトのまま続けるか社内ツールに閉じるか」の 1 点
- ブランチは `claude/dimming-system-simulation-i8xn8y`。`dev_v0.1` 経由で `main` へ（`main` への push = 本番デプロイ）
