# 引き継ぎ — Step 22 まで完了。次スコープは未確定

> このファイルは作業単位ごとに上書き更新される。プロダクト全体の要件は `requirements_definition.md`、技術設計は `design.md` を参照。
>
> **今は作業スコープが決まっていない状態。** 下の「次スコープの候補」からユーザーと決めてから着手すること。

## 到達点

**Step 0〜22 が完了し、本番へ出ている。**

| | 内容 |
|---|---|
| `main` | `27eecb5`（Step 18〜22 入り）。本番デプロイ済み（Version ID `45cc6ee7`） |
| `dev_v0.1` | `f64cf9c`。main ＋ `design.md` §9.1 の配布経路の記述 1 コミット |
| テスト | 534 passed（37 files） |

盤の写真（実機の調光システム）に対して、**制御回路・AC 負荷・調光の一連が動く**ところまで来ている。

- 実端子番号での配線（MY シリーズ・G7L・電磁接触器・タイマー）
- AC100V を電磁接触器で入り切り
- 0–10V の調光（逆特性 0V=100%）、位相制御調光器、16 回路の調光コントローラ
- カットリレー（明るさで動く接点）、調光操作卓（人の操作で動く接点）
- 診断 8 種（短絡・コイル極性・ダイオード逆・未接続・自己切断・発振・不収束・調光の基準不一致）
- 経路確認モード・電流の向き・励磁経路の言語化・ラダー図・部品交換・整列・Undo・モバイル

## すぐ片付けること（宿題 2 つ）

### ① Cloudflare Workers Builds の解除を確認する

**ユーザー側のダッシュボード操作待ち。** 同じ Worker への配布経路が 2 本あり、Cloudflare 側の Workers Builds は切ると決めた（理由は `design.md` §9.1）。

```
Cloudflare → Compute (Workers) → relay-lab → Settings → Build → GitHub 連携を Disconnect
```

**まだ切れていないと、次に main へ push したときまた「build token has been deleted or rolled」で落ちる。** 落ちても GitHub Actions 側で配布は成立するので実害は無いが、赤いビルドが残る。

**背景（同じ轍を踏まないため）:** Workers Builds は Actions Secrets とは別に自分のビルドトークンを握っている。Cloudflare 側でトークンを Roll すると Workers Builds だけが無効になる。前回これを踏んだ。

### ② `dev_v0.1` の 1 コミットを main へ

`f64cf9c`（`design.md` §9.1 の更新）がドキュメントだけの差分として `dev_v0.1` に残っている。次のリリースに乗せてよい。急ぐ理由は無い。

## 次スコープの候補（未確定・ユーザーと決めること）

### A. 調光コントローラの ON/OFF 出力 24–39 を繋ぐ

端子は出してあるが働いていない。**16 回路それぞれの駆動源（シーン記憶・フェーダー割付）は操作卓の内部ロジックで、機器 1 台では決まらない。** 操作卓とコントローラを通信で結ぶモデルが要る（`design.md` §4.16 末尾）。いちばん大きい。

### B. フェード（時間をかけた明るさの変化）

実機は「1 秒でフェードイン」のように連続して変わる。**既存のタイマー基盤がそのまま土台になる** —— `nowMs` を入力で受ける・実行時状態を返して次回入力に繋ぐ・`nextEventAtMs` で再計算の要否を決める（`design.md` §5.13）。違いは次のイベント時刻が離散から連続になる点だけ。

### C. ログイン（Supabase Auth + RLS）

方式は決定済み。**未決は 1 点だけ** —— relay-lab を公開プロダクトのまま続けるか、社内ツールに閉じるか。社内ツールでよいなら **Cloudflare Access が実装ゼロ**で済む（ただしサイト全体が社内限定になり `requirements_definition.md` §2 の読者に届かなくなる）。

着手時は `CLAUDE.md`（バックエンド・ログイン・DB は持たない）と `requirements_definition.md` §7・§8 を同じ作業の中で書き換える。エンジンへの影響は無い（`simulate()` は定義レジストリを引数で受け取るので、取得元が増えても無傷）。

### D. 3 ポジションセレクタ・DIP スイッチ

`kind: "switch"` は「端子 2 個 × 状態 2 つ」固定。実機の DIRECT/OFF/CUT のような 3 ポジションが表せない。**「ポジション列 × 各ポジションで閉じる端子ペア」に一般化**すれば、既存の NO/NC はポジション 2 つの特殊形として書き直せて、エンジンの分岐は増えない（`graph.ts` の `closedContactPairs()` と同じ形に収まる）。

### E. ブレーカー・サーマルリレー

3 極が連動する 1 つのスイッチ。D の一般化が前提。

## 扱わないと決めているもの

PWM と位相制御の**波形**、通信（RS-485 / DMX）の中身、フォトカプラ入力、三相・接地（FG）、定格容量の超過判定、シーン記憶・フェーダー操作の内部ロジック。

---

## 引き継ぎメモ（リポジトリの外の決定事項）

### 実機の仕様書について

- 社内資料。**型番・製造元・製品名はリポジトリに書かない**（ユーザー判断）。端子番号と挙動だけを実機に合わせる
- `lighting-system.test.ts` が「型番が漏れていないこと」を機械的に検査している（`/FMD|FDR|ファンテックス/i` に一致しないこと）
- 出典には**資料の版と日付**を残す。名前を伏せたぶん、これが無いと後から再検証できない
- 調光の逆特性（0V=100%）は仕様書に明記あり。ただし実機は 3 機種とも極性が**切替式**なので、定義に焼き付けずインスタンスの `dimmerSettings` が持つ

### パレットの分類方針

**パレットは電気的な分類ではなく「探す場所」。** 調光の機器は電気的にリレー／スイッチでも `category: "dimmer"` にまとめる —— MY4N を探している人のリレー一覧に混ぜないため。`kind` は変えないのでエンジンの扱いは同じ（`design.md` §4.16）。

ユーザーは「リレーシミュに調光を足して壊れないか」を気にしていた。**エンジンは汚れていない**ことをテストで示せる —— 調光を入れる前後で `relay.test.ts`・`chatter.test.ts`・`self-hold.test.ts`・`g7l-scenarios.test.ts` は差分ゼロ。モード切替は入れない判断をした（保存回路との衝突・エンジンの分岐・テスト面積で高くつく）。

### ブランチとデプロイ

- 作業は `dev_v0.1`。`main` へは PR で上げる（**`main` への push = 本番デプロイ**）
- 配布経路は **GitHub Actions の 1 本だけ**（`design.md` §9.1）
- `CLOUDFLARE_API_TOKEN` は Actions Secrets に設定済み・動作確認済み

### 渡したサンプル調光回路（リポジトリには無い）

動作確認用に作って**ファイルとして渡した**が、コミットしていない。scratchpad は消えるので、必要なら下の仕様から作り直す（`parseDocument()` → `simulate()` で検証済み）。

| 部品 ID | 定義 ID | 設定 |
|---|---|---|
| PS1 | `power-ac100v` | |
| PS2 | `power-dc24v` | |
| DIMC | `dimming-controller-16ch` | `channelVolts: { "1": 3 }` |
| S1 | `switch-selector-no` | DIRECT 用 |
| D1 | `dimmer-phase-control-ac100v` | |
| L1 | `lamp-ac100v` | |
| LC | `light-controller-4ch` | `triggerPercents: { "c1": 40 }` |
| L2 | `lamp-dc24v` | `lampColor: "red"` |

配線：`PS1:L–D1:IN` / `PS1:N–D1:COM` / `D1:OUT–L1:1` / `L1:2–PS1:N` / `DIMC:1–D1:CN` / `DIMC:21–D1:GND` / `DIMC:1–S1:1` / `S1:2–DIMC:21` / `DIMC:1–LC:IN1` / `DIMC:44–LC:ING` / `PS2:plus–LC:24V` / `PS2:zero–LC:GND` / `PS2:plus–LC:CRG` / `LC:CR1–L2:1` / `L2:2–PS2:zero`

挙動（実測）：0V→100% ／ 5V→50% ／ 8V→20% でカットリレー動作し L2 点灯 ／ 10V→0% ／ S1 を倒すと信号線が基準へ落ちて 100%。
