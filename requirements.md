# 作業単位の要求 — VP・スクリーン・モニターを部品として追加する

> このファイルは作業単位ごとに上書き更新される。プロダクト全体の要件は `requirements_definition.md`、技術設計は `design.md` を参照。
>
> 前の作業単位（Step 0〜24）は完了済み。直前の Step 24 で、操作卓の通信（フェーダー・照明スイッチ）をコントローラの 0–10V 出力へ届けた。

## 背景 — コントローラの端子 32〜39 の先につながる実機がまだ無い

調光コントローラ（`dimming-controller-16ch`）は端子 32・34〜36・38・39（電源 ON/OFF 出力・昇降 上昇/停止/下降出力・電源制御 NO/NC）を持つが、Step 21 で「端子として出すが、まだ接点としては働かない」と決めたまま、Step 24 でも扱わずに残していた（requirements.md の「含まないもの」）。

その先につながる実機（プロジェクター・電動スクリーン・モニター）を部品として盤に置けるようにするのが今回のスコープ。

## 今回のスコープ

**Step 25 — プロジェクター（VP）・電動スクリーン・モニターを部品として追加する。**

### 含むもの

- **プロジェクター（Panasonic PT-VX430J）** — 利用者提供の公式仕様書で確認。AC100V の負荷（`kind: "lamp"`）として表す。この機種は無電圧接点の制御端子を持たず、外部制御はシリアル入力端子（D-Sub 9P・RS-232C 準拠）のみ
- **VP コントローラー**（実機を持たない仮の中継機器） — 無電圧接点でコイルが励磁し、その先のプロジェクターへシリアルコマンドで電源 ON/OFF を送る想定。コイル 1 個・接点 0 個
- **モニター（汎用）** — AC100V の負荷。電源 ON/OFF は外部のリレー／電磁接触器でラインを直接断続する構成（本体に制御端子は持たせない）
- **電動スクリーン（汎用）** — COM・UP・STOP・DOWN の無電圧接点入力＋モーター用 AC100V。上昇・停止・下降は独立した 3 系統
- **エンジンの新機能：補助コイル（`RelayDefinition.auxCoils`）** — 1 台が複数の独立した無電圧接点入力を持つ機器（電動スクリーン）のために追加。主コイルとは別に、2 端子間の電位だけで励磁を判定する。接点は動かさず、`SimulationResult.auxCoilsEnergized` として結果に出るだけ（design.md §5.21）
- 新カテゴリ `"av"`（AV機器）とパレットへの表示

### 含まないもの

- **調光コントローラの端子 32〜39 を実際に接点として働かせること。** `analog-source` に接点を持たせる構造判断が要り、Step 21・24 に続いてなお次スコープに残る。今回追加した VP コントローラー・電動スクリーンは、現状は外部の電源と接点で個別に駆動して使う
- **電動スクリーンの上昇・下降・停止のインターロック。** 実機の仕様が不明なため、3 系統は完全に独立したまま（design.md §6 の既知の制約に追記）
- **シリアル通信（RS-232C）の中身。** VP コントローラーからプロジェクターへの信号は、コイルの励磁状態（プロパティパネルで確認）が唯一の観測対象で、通信のフレーム・コマンド自体は扱わない（CLAUDE.md「プロトコルは扱わない」）
- モニターの型番・実端子番号（ユーザー判断で汎用機器とした）

## ユーザーストーリーと受け入れ条件

### US-AX AV機器を盤に置ける

**盤を再現するユーザーとして、** プロジェクター・モニター・電動スクリーンをパレットから置きたい。

- パレットの「AV機器」からプロジェクター（PT-VX430J）・VP コントローラー・モニター・電動スクリーンを置ける
- プロジェクター・モニターは AC100V を繋ぐと負荷として成立する
- 電動スクリーンは COM と UP／STOP／DOWN のいずれかに電圧をかけると、その系統の補助コイルだけが励磁する（他の系統には影響しない）
- VP コントローラーは A1-A2 に電圧がかかるとコイルが励磁する（プロパティパネルで確認できる）
