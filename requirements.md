# 作業単位の要求 — 調光をフェード（時間をかけた変化）に対応させる

> このファイルは作業単位ごとに上書き更新される。プロダクト全体の要件は `requirements_definition.md`、技術設計は `design.md` を参照。
>
> 前の作業単位（Step 0〜20）は完了済み。部品を配置し、実端子番号どうしを配線し、▶ で動作を確認でき、保存・書き出し・Undo・診断・部品検索・範囲選択・部品交換・操作ヘルプ・停止中の配線チェック・電流の向き・励磁経路の言語化・接点の図記号・タイマー（限時）・モバイル表示・部品の整列・経路確認モード・配線の部品回避・表示ランプの色・ラダー図への変換・電磁接触器と AC 電源・**0–10V の調光**まで揃っている。

## 背景 — なぜこれをやるのか

Step 20 で調光そのものは入った。0–10V のアナログ量を導通レイヤに重ねる層（`design.md` §5.17）ができ、0V = 100% の逆特性、挿し忘れ＝全灯の警告、接点で 0V に落とす DIRECT、専用の配線色までが動く。

**足りないのは時間。** 実機の調光は「10V から 0V へ一瞬で飛ぶ」のではなく、秒単位でなめらかに変わる。盤の説明として「ボタンを押すと 3 秒かけて明るくなる」が図に描けないと、調光盤を再現したとは言えない。

**土台はすでにある。** タイマー（`design.md` §5.13）が「`nowMs` を入力で受ける・実行時状態を返して次回入力に繋ぐ・`nextEventAtMs` で再計算の要否を決める」という形を作っており、フェードはその 3 点セットをアナログ層でもう一度使うだけになるはず。**ここが素直に載らなかったら、時間の扱いの設計が足りていないサイン**（Step 7 の「エンジンの差分 0 行」と同じ種類の検証を兼ねる）。

## 今回のスコープ

**Step 21 — 調光出力にフェード時間を持たせ、明るさが時間をかけて変わるようにする。**

### 決まっていること

**① フェードは調光出力の性質。ランプ側ではない。**

なめらかに変えているのは調光器であって、受け側の機器は今この瞬間の電圧を見ているだけ。`AnalogCurve`（V → %）は受け側、フェード時間は出力側 —— **層を跨がせない。** 跨がせた瞬間に「どちらがフェードを持つのか」という二重の真実ができる。

**② 実行時状態は `TimerState` と同じ形で持つ。**

`SimulationResult` が返し、次回の `SimulationInput` に戻る。**`changedAtMs: number | null` の `null` が「開始からずっとこの値」を意味する約束（§5.13）もそのまま借りる** —— 0 で初期化すると、まだ一度も動かしていない調光器が開始直後にフェードを始める。

**③ `nextEventAtMs` を共有する。**

タイマーとフェードのどちらが先に動くかを `simulationStore` が知る必要はない。**いま `nextEventOf()` が返している「次に接点が変わる時刻」に、フェードが変わる時刻も混ぜる。** 2 本に分けると片方だけ見て再計算を止める事故が起きる。

**④ 停止中は目標値をそのまま出す。**

フェードは時間の話なので、▶ を押していないときに途中の値を出す意味が無い（タイマーの残り時間を停止中に出さないのと同じ）。プロパティパネルの「出力」欄も目標値のまま。

**⑤ DIRECT（接点で 0V へ落とす）がフェードするかを決めて書く。**

Step 20 では「信号ネットと基準ネットが同じネットなら 0V」というネットの形からそのまま出る答えにしてある（§5.17）。フェードを入れると、**この経路が瞬時なのか設定時間をかけるのかを選ばないといけない。** 実機の DIRECT は調光器を迂回して信号線を直接落とす配線なので**瞬時**が素直だが、どちらにせよ理由を `design.md` に書き残すこと。

### 含むもの

- 調光出力の定義にフェード時間（`fadeMs`）を持たせ、インスタンスで上書きできるようにする（`presetMs` と同じ扱い）
- アナログ層に実行時状態を足し、`nowMs` から今の電圧を求める
- `nextEventAtMs` にフェードの終了時刻を混ぜる
- フェード中であることが画面から読める（**タイマーの「残り」と同じ扱い**）
- DIRECT がフェードするかどうかを決めて書く

### 含まないもの

- **FMD-701D そのものの定義** —— 下記の未決事項が要る（Step 20 から持ち越し）
- **ログイン（Supabase Auth + RLS）** —— 社内部品を配る前に要るが、フェードとは独立
- フェードのカーブ（対数調光・S 字）—— 直線で始める。カーブが要るなら `AnalogCurve` と同じく定義側の宣言にする
- 通信（RS-485 / DMX）・位相制御・力率・消費電力の数値
- 三相・接地（FG）

## ユーザーストーリーと受け入れ条件

### US-AO 明るさが時間をかけて変わる

**盤を図にするユーザーとして、** 実機と同じようになめらかに明るくなる様子を見たい。

- 調光出力にフェード時間を設定でき、▶ 中に出力を変えると設定時間をかけて変わる
- 途中の明るさが端子（V）と部品（%）の両方に出る
- フェード中であることが画面から読める
- 停止中は目標値がそのまま出る

### US-AP フェードとタイマーが同じ時間軸に乗る

**回路を確かめるユーザーとして、** タイマーとフェードが同時に動いても取り違えたくない。

- タイマーの限時とフェードが同じ回路で同時に動く
- どちらか一方しか無い回路でも、再計算のループが止まらない／回りっぱなしにならない
- `simulate()` は時計を読まない（`nowMs` を入力で受ける）ままである

## 完了判定

- `npm test` が緑
- 上記 US-AO・AP が動く
- `design.md` §3・§4.14・§5.13・§5.17・§6 と `requirements_definition.md` §6.1・§8 が更新されている
- **`design.md` §6-3 と `src/lib/help-content.ts` の `LIMITATIONS` が同じ作業の中で書き換わっている** —— 現状の「調光は 0–10V の値だけ。フェードも通信も扱わない」がそのまま残ると、画面とドキュメントが実装と食い違う

---

## 引き継ぎメモ（リポジトリの外の決定事項）

コードにもドキュメントにも書けないが、次の判断に要るもの。

### FMD-701D について（Step 20 から持ち越し・未着手）

- **社内オリジナル製品。** 公開データシートは存在しない
- **リポジトリ・公開サイトに載せてよい**（ユーザー確認済み）
- **調光仕様は 0V = 100% / 10V = 0%**（Step 20 の汎用部品にこの向きで入っている）
- 実端子記号を主張するときの一次資料は**社内図面**になる。`source` に図面番号・版数・確認日を残さないと後から再検証できない（CLAUDE.md 設計原則 5。社内製品は改訂されるので外部品より重要）

**着手前に確定が要る 3 点**（Step 20 の時点から動いていない）。

1. **接点入力（SW1… 端子）が何をするか** —— どの入力でどのチャンネルがどのレベルになるか
2. **信号線が未接続のときのレベル** —— 実機の入力回路がプルアップかプルダウンか（汎用部品は `unconnectedVolts: 0` ＝全灯で置いてある）
3. **DIRECT / CUT が信号線に何をするか** —— 0V に落とすのか、別系統に切り替えるのか

**汎用部品の側はもう用意できている。** `definitions/dimming.ts` の 2 部品を写して端子と `source` を差し替えれば足り、`engine/` は 1 行も変わらない（Step 20 で確認済み）。

### ログインについて

- **目的は「社内部品を見せる相手を絞ること」だけ**（ユーザー選択済み）
- **方式は Supabase Auth + RLS で確定。** 静的書き出し（`output: "export"`）のままログインを付けられ、`wrangler.jsonc` の前提を壊さない
- **クライアント側ログインだけでは守れない。** 静的 SPA なので、定義を TypeScript に書けば JS チャンクで全訪問者に配られる。守るには定義をサーバ側（Supabase）に置いて RLS を張るしかない
- **未決の 1 点** —— relay-lab を公開プロダクトのまま続けるか、社内ツールに閉じるか。社内ツールでよいなら **Cloudflare Access が実装ゼロ**で済む（ただしサイト全体が社内限定になり、`requirements_definition.md` §2 の読者に届かなくなる）
- 着手時は `CLAUDE.md`（バックエンド・ログイン・DB は持たない）と `requirements_definition.md` §7・§8 を同じ作業の中で書き換える
- エンジンへの影響は無い。`simulate(doc, defs, input)` は定義レジストリを引数で受け取り、`ComponentDefinitionRegistry` はただの `ReadonlyMap` なので、取得元が増えてもエンジンもテストも無傷

### ブランチとデプロイ

- 作業は **`dev_v0.1`**。`main` へは PR で上げる（`main` への push = 本番デプロイ）
- **CI 経由のデプロイはまだ一度も成功していない。** `CLOUDFLARE_API_TOKEN` を Actions Secrets に登録済みだが、run #11 は attempt 1 のまま failure で、再実行はされていない。本番へは手元から配った（`npm run deploy`）。**次に `main` へ push したときが、CI 経路の初めての検証になる**
