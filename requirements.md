# 作業単位の要求 — 配線が部品を跨がないようにする／表示ランプの色

> このファイルは作業単位ごとに上書き更新される。プロダクト全体の要件は `requirements_definition.md`、技術設計は `design.md` を参照。
>
> 前の作業単位（Step 0〜16）は完了済み。部品を配置し、実端子番号どうしを配線し、▶ で動作を確認でき、保存・書き出し・Undo・診断・部品検索・範囲選択・部品交換・操作ヘルプ・停止中の配線チェック・電流の向き・励磁経路の言語化・接点の図記号・タイマー（限時）・モバイル表示・部品の整列・経路確認モード（スイッチ操作つき）まで揃っている。

## 今回のスコープ

**Step 17 — 線が部品の上を通らないようにし、表示ランプのレンズの色を選べるようにする。**

独立した 2 件だが、どちらも**図面として読めるか**という同じ問題に属する。

### ① 配線が部品を跨ぐ

`design.md` §8.7 は配線どうしの重なりだけを解いており、**部品の本体を跨ぐことは想定していなかった。** 線が本体の上を通ると、線と型番・端子番号のどちらも読めなくなる —— 実端子番号を読ませることがこのプロダクトの価値そのものなので、これは見た目の問題では済まない。

とくに **同じ部品の上辺と下辺を結ぶ線**は、両端の座標が揃うため `getSmoothStepPath` が直線を返し、本体を真っ直ぐ縦断する。

### ② 表示ランプの色が 1 色しかない

制御盤では赤＝異常・緑＝運転・黄＝警報という対応が慣習で、**色そのものが図面の情報。** 表示ランプが全部同じ色だと、その意味を図に書けない。

### 含むもの

- **幹線・走行が部品の矩形に入っていたら外へ逃がす**（近いほう・最大 320px）
- 同じ部品を避ける配線どうしを、避けたあとも離しておく
- 表示ランプのレンズの色（黄・赤・緑・青・白）をプロパティパネルで選ぶ
- **消灯中も色が分かる**描き方
- 選んだ色の保存・読み込み・Undo

### 含まないもの

- **配線どうしの交差を無くすこと** —— §8.7 冒頭の判断（レーン分離＋強調で扱う）は変えない。避けるのは部品だけ
- **端子から出る助走・迂回の折れ部分まで避けること** —— 逃がすのは長い直線区間（幹線・走行）だけ
- **回り込めないときに無理に引き回すこと** —— 320px を超えるなら跨いだままにする
- 部品の色・電源の色など、ランプ以外の見た目の設定
- ランプの色による動作の変化（電気的な意味は持たせない）

## ユーザーストーリーと受け入れ条件

### US-AF 線が部品の上を通らない

**図面を読むユーザーとして、** 型番と端子番号を線に隠されたくない。

- 同じ部品の上辺と下辺を結ぶ線が、本体を縦断せず外を回り込む
- 電源と負荷の間に部品が挟まっていても、走行がその本体を横切らない
- 逃がす先は本体から 16px 離れる（端子から出る助走と重ならない）
- **横切っていない配線は 1px も動かない**
- 同じ部品を避ける配線が複数あっても、避けたあとに重ならない
- 回り込む幅が足りなければ跨いだままにする（折り返した線を作らない）

### US-AG ランプの色で意味を書ける

**盤図を清書するユーザーとして、** 赤と緑を描き分けたい。

- ランプを選ぶとプロパティパネルに「レンズの色」が出る（ランプ以外では出ない）
- 黄・赤・緑・青・白から選べ、**色見本と色名の両方**が並ぶ
- **消灯中もレンズの色が分かる**（淡い塗り）
- 点灯すると濃い塗り＋縁＋光芒になり、色相に頼らず点灯が読める
- 白いランプも点灯が分かる
- 色を変えても点灯条件は変わらない
- Undo 1 回で戻り、保存・読み込みで保たれる
- 色を選んでいない既存の回路は見た目が変わらない

## 完了判定

- `npm test` が緑（`wire-lane.test.ts` と `document-storage.test.ts` の追加分を含む）
- `npm run typecheck` と `npm run build` が通る
- `design.md` §4.11・§8.7「部品の本体を跨がない」・§8.3「レンズの色」に設計判断が残っている
