# 作業単位の要求 — 操作卓の通信でコントローラの出力を動かす

> このファイルは作業単位ごとに上書き更新される。プロダクト全体の要件は `requirements_definition.md`、技術設計は `design.md` を参照。
>
> 前の作業単位（Step 0〜23）は完了済み。直前に、カットリレーの動作点（`triggerPercents`）が保存もパネル入力もできていなかった欠落を潰した。

## 背景 — 盤の中心がまだ繋がっていない

実機の盤は **操作卓が頭、コントローラが出力段**で、その間を通信線（端子 7・8 ↔ 22・23）が結んでいる。操作卓のフェーダーを上げると、コントローラの 0–10V 出力が動き、その先の位相制御調光器が照明を明るくする。

いまはこの**真ん中が繋がっていない。**

| いまの状態 | |
|---|---|
| コントローラの 16 回路 | インスタンスの `channelVolts` を**手で入れる**しかない |
| 操作卓のボタン | 自分の無電圧接点しか動かさない |
| 通信線（7・8 / 22・23） | 端子として引けるが**何も起きない** |

盤を図にしても「操作卓を触ったら照明が変わる」という一番見たい動きが出ない。

## 今回のスコープ

**Step 24 — 通信で操作卓の操作をコントローラの出力へ届ける。**

### 再現する範囲（ユーザー決定）

**フェーダー 1–8 と 照明スイッチ 1–8 まで。**

| 操作卓の操作子 | コントローラの出力 |
|---|---|
| フェーダー 1–8（0–100%） | 端子 1–8 の 0–10V |
| 照明スイッチ 1–8（入り切り） | 端子 9–16 の 0–10V |

**電源・VP・スクリーン・AUX（端子 32–39）と ON/OFF 出力（端子 24–31）は含まない。** 前者は操作子と出力の対応表が大きくなるだけ、後者は `analog-source` に接点を持たせる構造判断が要る（下記）。

### 通信は電気モデルに参加させない

**`ComponentDefinition` の `electrical` の中には入れない。** 通信線が運ぶのは電位ではなく値で、ネットの分割にも `NetState` にも関係が無い。`electrical` と並ぶ別の面として `communication` を持たせる。

送り手と受け手は**共有する名前**（`fader1`・`light1` …）で対応させる。エンジンは名前を突き合わせるだけで、どちらの機器かは見ない（CLAUDE.md 設計原則 2）。

### 通信が届いていないときの扱い（ユーザー決定）

**インスタンスの手動設定（`channelVolts`）をそのまま使い、通信が届いたら通信が勝つ。**

- 保存済みの回路と、操作卓を置かない回路が**今までどおり動く**
- そのうえで、**配線の不備を停止中の配線チェックと実行中の警告に出す**
  - 通信線が片側しか繋がっていない
  - ＋と − が逆に繋がっている（A/B 逆結線）
  - **GND が共通でない** —— 差動信号は基準を共有していないと成立しない

### 含むもの

- `ComponentDefinition.communication`（ポート・送る値・受けて割り当てる先）
- 操作卓の操作子に**連続量**を足す（フェーダーは 0–100%。いまの操作は入り切りだけ）
- 操作卓に フェーダー 1–8・照明スイッチ 1–8 を持たせ、通信で送る
- コントローラが受けて、端子 1–16 の出力電圧を決める
- **% → V の変換はコントローラ側の設定**（この盤は 0V = 100% の逆特性。実機も調整ボリュームで合わせるもの）
- 通信配線の警告 3 種
- 操作卓のフェーダーを画面から動かせるようにする

### 含まないもの

- **ON/OFF 出力（端子 24–31）** —— オープンコレクタは接点で、`analog-source` の機器に接点を持たせる構造判断が要る。値だけを送る今回とは別の話なので次スコープ
- 電源・VP・スクリーン・AUX（端子 32–39）
- **シーン記憶と呼び出し** —— 記憶した内容を保存するかどうかが新しい問題になる（盤の状態は配線ではないので保存対象外にしてきた）
- フェーダーの自動／手動割付、PWM 出力の割付などのパラメータ設定
- **通信プロトコルそのもの** —— フレーム・アドレス・ボーレートは扱わない。運ぶのは「どの名前がいくつか」だけ
- 終端抵抗（抵抗器の部品that自体が無い）

### 操作の状態は保存しない

フェーダーの位置も照明スイッチの入り切りも `SimulationInput` で受け、`CircuitDocument` には持たない。**オルタネートスイッチ・操作卓のボタンと同じ扱い**で、■ で停止すると既定へ戻る（`design.md` §4.7・§4.16）。

## ユーザーストーリーと受け入れ条件

### US-AU フェーダーで照明の明るさが変わる

**盤を再現するユーザーとして、** 操作卓を触ったら照明が変わる様子を見たい。

- 操作卓とコントローラを通信線（＋・−・GND）で繋ぐ
- 操作卓のフェーダー 1 を動かすと、コントローラの端子 1 の電圧が変わる
- その先に位相制御調光器と照明を繋いでおけば、**明るさが追随する**
- フェーダー 1–8 が独立して効き、それぞれ端子 1–8 に対応する
- **0% で消灯側（10V）、100% で全灯側（0V）**（この盤の逆特性）

### US-AV 照明スイッチが入り切りで効く

- 照明スイッチ 1–8 を倒すと、コントローラの端子 9–16 が全灯側／消灯側へ振れる
- 倒していないときは消灯側

### US-AW 通信線の配線ミスが分かる

**配線を確認するユーザーとして、** 繋いだつもりで繋がっていない状態を先に知りたい。

- ＋しか繋いでいないと「通信線が片側しか繋がっていない」と出る
- ＋と − を逆に繋ぐと「A/B が逆」と出る
- **GND を共通にしていないと「基準が共通でない」と出る**（差動信号は基準を共有しないと成立しない）
- どの場合も**通信は成立せず**、コントローラは手動設定のまま
- **▶ を押す前（停止中の配線チェック）にも出る**

### US-AX 通信を使わない回路が壊れない

- 操作卓を置かない回路では、コントローラは今までどおり `channelVolts` の手動設定で動く
- 保存済みの回路を開いても挙動が変わらない
- 通信を使わない回路の答え・警告・配線色が 1 つも変わらない

## 完了判定

- `npm test` が緑
- US-AU〜AX が動く
- `design.md` §3.1・§4.15・§4.17・§5.19 と `requirements_definition.md` §6.1 が更新されている
- **既存のテストの期待値を書き換えずに通る**（書き換えが要るなら、それは通信を使わない回路の挙動を変えたということ）

---

## 引き継ぎメモ（リポジトリの外の決定事項）

- 実機の仕様書は社内資料。**型番・製造元・製品名はリポジトリに書かない。** 端子番号と挙動だけを実機に合わせる
- 調光の逆特性（0V=100%）は仕様書に明記あり。ただし実機は極性切替式なので、インスタンス設定が持つ
- ログイン：Supabase Auth + RLS で確定。未決は「公開プロダクトのまま続けるか社内ツールに閉じるか」の 1 点
- ブランチは `dev_v0.1`。`main` への push = 本番デプロイ。**PR を出す前に毎回 `git merge origin/main` で揃える**
- Cloudflare Workers Builds の解除はユーザー側の宿題（未確認）。解除前は main への push でビルドエラーが出るが無害
