# 引き継ぎ — Step 23（調光のフェード）完了。次のスコープは未確定

> このファイルは作業単位ごとに上書き更新される。プロダクト全体の要件は `requirements_definition.md`、技術設計は `design.md` を参照。
>
> **いまは「次に何をやるか」が決まっていない状態。** 着手前にユーザーへ確認すること。候補は下の「次スコープの候補」。

## いまどこまで出来ているか

Step 0〜23 が完了。**本番に出ているのは Step 22 まで**（Step 23 は `dev_v0.1` にあり、`main` へは未マージ）。

| 層 | 状態 |
|---|---|
| 接点で組んだ制御回路（リレー・接触器・タイマー） | 完了 |
| AC100V を電磁接触器で入り切りする負荷側 | 完了 |
| 0–10V のアナログ量（調光） | 完了 |
| 実機の調光機器（コントローラ・位相制御調光器・ライトコントローラ・操作卓） | 完了 |
| アナログ ⇄ 接点の橋渡し（カットリレー・操作卓のボタン） | 完了 |
| **調光のフェード（時間をかけた明るさの変化）** | **完了（Step 23）** |

部品は 24 種。診断は 8 種（短絡・コイル極性・ダイオード逆・未接続・自己切断・発振・不収束・調光の基準不一致）。

```
npm test  569 passed (38 files)   ← Step 22 時点の 534 件は期待値を 1 つも書き換えずに通っている
npm run build  静的書き出し成功
```

## Step 23 でやったこと（フェード）

調光出力が出す 0–10V が、設定した時間をかけて目標値へ動く（`design.md` §5.18）。

- **フェードするのは出力する電圧**。受け側の入力段は 1 行も変えていない。実機で時間をかけているのはコントローラで、位相制御調光器は来ている電圧に追従しているだけ
- したがって **接点で 0V へ落とす配線（DIRECT）は瞬時のまま**（機器の外の短絡で出力段を通らない）。配線の V ラベルは `analog.signalOf` を読んでいるので無変更で滑らかに動く。カットリレーはフェード中に動作点をまたいで動く
- 状態は `FadeState`（目標・開始値・開始時刻）の 3 つで、**今の電圧は保持せず導く**。`TimerState` とまったく同じ構え
- **既定は 0 秒（フェードしない）。** 保存済みの回路を開いた瞬間に挙動が変わらないことを最優先にした。プロパティパネルで秒を入れて初めて効く
- `nextEventAtMs` の意味が「次に接点が変わる時刻」（離散）から「次に結果が変わり終わる時刻」（離散＋連続）へ広がったが、**`simulationStore` のロジックは 1 行も変えていない** —— あそこが読むのは「まだ動いているか」の 1 ビットで、刻みは固定の 50ms だから

**上げと下げは共通の 1 つの時間**（ユーザー決定）。分けたくなったら `FadeSpec` を 2 組にするだけで、エンジンの式は 1 本のまま使える。

## リポジトリの状態（※ここを最初に確認すること）

```
origin/main      27eecb5   Step 20〜22 マージ済み・本番デプロイ済み
origin/dev_v0.1  main を取り込み済み。Step 23 とドキュメント 2 件が main に未反映
```

**`dev_v0.1` は main を取り込み済み**（マージコミットあり）。この状態を保つこと —— 以前これを怠って `design.md` の Step 番号が衝突した（`git log` の「main を取り込む」コミット参照）。`dev_v0.1` → `main` の PR を出す前には毎回 `git merge origin/main` で揃える。

作業ブランチは `dev_v0.1`。`main` への push はそのまま本番デプロイ。

**`claude/*` の作業ブランチは無視してよい。** `claude/dimming-system-simulation-i8xn8y` は `dev_v0.1` の履歴に完全に含まれており（先行コミット 0 件）、衝突しない。

## 未処理の宿題

### ① Cloudflare Workers Builds の解除（ユーザー側の操作・未確認）

配布経路を **GitHub Actions の 1 本に絞る**と決めた（`design.md` §9.1 に理由を記載）。ダッシュボードでの解除をユーザーに依頼済みだが、**完了は確認できていない。**

```
Cloudflare → Compute (Workers) → relay-lab → Settings → Build → GitHub 連携を Disconnect
```

解除されていれば、main への push で `deploy.yml` だけが走る。**解除前は Workers Builds のビルドエラーが出るが、それは無害**（GitHub Actions 側が実際の配布を完了させている）。

### ② `triggerPercents` が保存されず、UI からも設定できない（Step 23 で発見・未着手）

**カットリレーの動作点が実質固定になっている。**

`src/circuit/engine/relay.ts:186` は `instance.triggerPercents` を読んでいるが、書き手がいない。

- `persistence/document-storage.ts` の部品パースに項目が無く、**保存・読み込みで黙って消える**
- `PropertiesPanel` に入力欄が無い（`presetMs` / `channelVolts` / `fadeMs` にはある）

結果、動作点は常に定義の `defaultBelowPercent`（25%）に落ちる。下のサンプル回路が「`LC` の `triggerPercents: { "c1": 40 }` が初期値」と書いているのは**テストの中だけで成立している状態**。

直し方は決まっている —— `readTriggerPercents`（`readChannelVolts` と同形）＋ パネルの入力欄（`channelVolts` の欄と同形）。**Step 23 のスコープ外としてユーザーの判断待ちのまま残してある。** 小さいので単独で 1 コミットにできる。

### ③ 実機の盤で、まだ再現できていないもの

- **調光コントローラの ON/OFF 出力 24–39**（オープンコレクタ）。端子は出しているが働かない。16 回路それぞれの駆動源がシーン記憶・フェーダー割付という**操作卓の内部ロジック**で、機器 1 台では決まらない。繋ぐには操作卓とコントローラを通信で結ぶモデルが要る
- **PWM の波形**（1kHz / 300Hz）・**位相制御の波形**
- **受け側のソフトスタート**（調光器そのものが時間をかける）。上げと下げで別のフェード時間
- 通信（RS-485 / DMX）、フォトカプラ入力、三相・接地、定格容量の超過判定

## 次スコープの候補（ユーザー未決）

| 候補 | 規模 | 備考 |
|---|---|---|
| **`triggerPercents` を通す**（上の宿題 ②） | 小 | 既存機能が半分しか繋がっていない状態の解消。永続化 1 箇所 ＋ パネル 1 箇所 |
| **3 ポジションセレクタ / DIP スイッチ** | 中 | `kind: "switch"` は端子 2 個・状態 2 つ固定。ポジション列 × 閉じる端子ペアへ一般化する |
| ブレーカー・サーマルリレー | 小〜中 | 3 極連動には上のスイッチ一般化が先 |
| 実型番の電磁接触器・タイマー | 小 | 公式カタログの図と照合する工程が要る |
| **ログイン**（Supabase Auth + RLS） | 大 | 方式は確定済み。下の「引き継ぎメモ」参照。ユーザーは後回しと判断済み |

---

## 引き継ぎメモ（リポジトリの外の決定事項）

### 実機の仕様書について

- 社内オリジナル製品。**型番・製造元・製品名はリポジトリに書かない**（ユーザー判断）。端子番号と挙動だけを実機に合わせる
- `lighting-system.test.ts` が「型番が漏れていないこと」を機械的に検査している
- 調光の逆特性（0V = 100%）は仕様書に明記あり。ただし**実機は 3 機種とも極性が切替式**なので、機器の仕様ではなく盤ごとの設定として `dimmerSettings` が持つ
- **フェード時間は仕様書から特定の秒数を読み取れていない。** だから定義の既定値は 0（フェードしない）にしてあり、盤ごとの設定として `fadeMs` が持つ。実機の値が分かったら定義ではなく「よく使う値」として案内する側に足すこと
- 出典には**資料の版と日付**を残す（型番を伏せたぶん、これが無いと再検証できない）

### ログインについて

- **目的は「社内部品を見せる相手を絞ること」だけ**（ユーザー選択済み）
- **方式は Supabase Auth + RLS で確定。** 静的書き出し（`output: "export"`）のままログインを付けられ、`wrangler.jsonc` の前提を壊さない
- **クライアント側ログインだけでは守れない。** 静的 SPA なので、定義を TypeScript に書けば JS チャンクで全訪問者に配られる。守るには定義をサーバ側（Supabase）に置いて RLS を張るしかない
- **未決の 1 点** —— 公開プロダクトのまま続けるか、社内ツールに閉じるか。社内ツールでよいなら **Cloudflare Access が実装ゼロ**（ただしサイト全体が社内限定になり、`requirements_definition.md` §2 の読者に届かなくなる）
- エンジンへの影響は無い。`simulate(doc, defs, input)` は定義レジストリを引数で受け取るので、取得元が増えてもエンジンもテストも無傷
- 着手時は `CLAUDE.md`（バックエンド・ログイン・DB は持たない）と `requirements_definition.md` §7・§8 を同じ作業の中で書き換える

### 「リレーシミュに調光を足して壊れないか」への回答（確認済み・フェードでも維持）

ユーザーから出た懸念。**エンジンは汚れていない**ことを検証済み。

- 調光を入れる前後で、リレー系のテスト（`relay.test.ts`・`chatter.test.ts`・`self-hold.test.ts`・`g7l-scenarios.test.ts`）は**差分ゼロ**。期待値を書き換えずに通っている
- **フェードを入れたときも同じ** —— Step 22 時点の 534 件が 1 件も期待値を書き換えずに通った。既定 0 秒という設計判断が機械的に効いている証拠になっている
- 調光部品を置かない回路では `resolveAnalog()` が空を返すだけ
- **モード切替（リレーのみ / リレー＋調光）は入れない判断。** 保存回路との衝突（部品を隠すと図と実配線がずれる）・エンジンの分岐・テスト面積の 3 点で高くつく
- 代わりにパレットの分類を直した（調光の機器は電気的にリレー／スイッチでも `category: "dimmer"`）。リレー一覧とスイッチ一覧は調光を入れる前と同じ並び
- 部品がさらに増えたら「パレットの絞り込み」（表示だけの状態でシミュレーションにも保存にも触れない）を検討する

### サンプル調光回路（ユーザーへ受け渡し済み・リポジトリには無い）

動作確認用に作って渡した JSON。**コンテナの scratchpad にあるため、セッションが変わると消える。** 再生成が要るときはこの構成で作り直す（`parseDocument()` → `simulate()` で検証済み）。

```
PS1 AC100V ─┬─ D1 位相制御調光器 IN
            └─ COM ─┬─ L1 AC100V照明 端子2
                     D1 OUT ─── L1 端子1
DIMC 調光コントローラ 回路1（端子1）─┬─ D1 CN
                                      ├─ S1 セレクタa接点 ─ DIMC 端子21（DIRECT）
                                      └─ LC ライトコントローラ IN1
DIMC 端子21 ─ D1 GND        DIMC 端子44 ─ LC ING
PS2 DC24V ─ LC 24V/GND、LC CRG ← PS2 plus、LC CR1 ─ L2 DC24V表示灯(赤) ─ PS2 zero
```

実測の挙動（`channelVolts` の回路 1 を振る）。

| 電圧 | L1 の明るさ | 点灯 | カットリレー |
|---|---|---|---|
| 0V | 100% | L1 | — |
| 5V | 50% | L1 | — |
| 8V | 20% | L1・L2 | 動作 |
| 10V | 0% | L2 のみ | 動作 |
| S1（DIRECT）を倒す | 100% | L1 | — |

`LC` の `triggerPercents: { "c1": 40 }`（動作点 40%）、`DIMC` の `channelVolts: { "1": 3 }` が初期値。**ただし `triggerPercents` は保存されないので、読み込むと 25%（定義の既定値）に戻る**（上の宿題 ②）。読み込むと未使用回路（LC の IN2–4・CR2–4）で `info` の未接続が 6 件出るが正常。

**フェードを見るには** `DIMC` のプロパティで「フェード」に 3 秒を入れ、▶ のまま回路 1 の V を 0 → 10 へ変える。配線の V ラベルと L1 の明るさが 3 秒かけて動き、途中で動作点をまたいだところで L2 が点く。
