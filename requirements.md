# 作業単位の要求 — MVP 仕上げ

> このファイルは作業単位ごとに上書き更新される。プロダクト全体の要件は `requirements_definition.md`、技術設計は `design.md` を参照。
>
> 前の作業単位（Step 0〜4「MVP 第一目標」）は完了済み。部品を配置し、実端子番号どうしを配線し、▶ で押しボタンを押すとリレーが励磁して接点が切り替わり、ランプが点灯するところまでが画面上で動く。

## 今回のスコープ

`design.md` §9 の **Step 5 〜 Step 7**。

「組んだ回路の中身を読み取れて、保存して続きから触れて、部品が増えてもエンジンが変わらない」ところまでを成立させ、MVP を完成させる。

### 含むもの

**Step 5 — プロパティパネル＋端子ツールチップ**

- 選択部品のコイル仕様・接点構成・励磁状態・接点ごとの COM/NO/NC 導通状態をシミュレーション中にリアルタイム更新する
- 端子ホバーで `TerminalDefinition.description` をツールチップ表示する（「端子 14 / コイル + / DC24V」）
- インスタンスのラベル（"RY1"）をパネルから変更できる

**Step 6 — 保存 / 読込 / Undo / Redo / 警告表示**

- `CircuitDocument` を LocalStorage へ保存・復元する。読み込み時に未知の `definitionId` を検証して弾く
- Undo / Redo（`{ past, present, future }`）。スナップショットは部品追加 / 削除 / 配線確定 / ドラッグ完了の 4 点のみ
- `SimulationResult.warnings` の UI 表示。`severity` で出し分け、該当部品を選択できるようにする

**Step 7 — 部品追加＋検索**

- MY2N DC24V（8 ピン・飛び番）／ MY4N-D2 DC24V（`polarity: "strict"`）／ 端子台 ／ ダイオード の定義追加
- パレットの部品検索 UI（型番・メーカー・カテゴリ）

### 含まないもの（MVP 後）

- 電圧・電流・消費電力の数値計算（`design.md` §6-3）
- ダイオードの整流作用（§6-2）。定義は追加するが MVP では常に開放のまま
- タイマーリレー・動作時間・チャタリング（§6-4）
- バックエンド / ログイン / 部品データの DB 移行

## ユーザーストーリーと受け入れ条件

### US-D 選択した部品の中身が読める

**ユーザーとして、** いま何が起きているのかを部品ごとに確認したい。

- MY4N を選ぶと、コイル仕様（DC24V / 極性）と 4 接点それぞれの COM–NO / COM–NC の導通状態が並ぶ
- シミュレーション中に押しボタンを押すと、その表示が即座に切り替わる
- 端子にホバーすると「端子 9 / 第1接点 COM」のように意味が読める
- 未検証の端子データであることがパネル上でも分かる（`verified` / `source`）

### US-E 続きから作業できる

**ユーザーとして、** 組みかけの回路を失いたくない。

- リロードしても直前の回路が復元される
- 誤操作を Undo で戻せる。1 回のドラッグで履歴が数百件積まれない
- 電源短絡・コイル極性逆・未接続端子・発振が画面上で確認でき、発振はエラーとして表示されない

### US-F 部品が増えてもエンジンが変わらない

**開発者として、** データ駆動設計が本当に機能しているか確かめたい。

- MY2N の端子番号が 1・4・5・8・9・12・13・14 の**飛び番のまま**表示される（1〜8 に詰め直さない）
- MY4N-D2 は逆極性で励磁せず、`error` の警告が出る。MY4N は逆極性でも励磁し `warning` に留まる
- 端子台は全端子が常時導通する
- **`src/circuit/engine/` を 1 行も変更せずに上記が成立する**

### 完了判定

```
検証回路テスト 1〜5 が緑のまま
  → MY2N / MY4N-D2 / 端子台 / ダイオードを追加
  → エンジンの差分が 0 行
  → 回路を組んでリロードし、復元される
  → Undo で 1 手ずつ戻る
```

## 制約事項

- **`src/circuit/engine/` に手を入れたら設計が破綻しているサイン**（`design.md` §9）。その場合は実装を止めて設計を見直す
- 新しい型番の端子データも `source` と `verified: false` を必ず記載する。MY2N / MY4N-D2 も実機／公式データシート検証待ち（`design.md` §4.4）
- Undo 履歴に `moveComponent` の毎フレーム更新を積まない（`design.md` §7）
- LocalStorage に実行時状態（`running` / `pressedSwitches` / `SimulationResult`）を保存しない
- 記述・UI とも日本語
