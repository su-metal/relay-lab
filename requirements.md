# 作業単位の要求 — 実機の調光システムを機器として置けるようにする

> このファイルは作業単位ごとに上書き更新される。プロダクト全体の要件は `requirements_definition.md`、技術設計は `design.md` を参照。
>
> 前の作業単位（Step 0〜20）は完了済み。Step 20 で 0–10V のアナログ量そのものは入った（`engine/analog.ts`・第 2 パス）。

## 背景 — 実機の仕様書が手に入った

これまで「汎用 0–10V 調光出力」と「汎用調光ランプ」という**最小限の 2 部品**でアナログ層を組み立てた。実機の仕様書（社内資料）が読めるようになったので、**盤に実際に載っている機器の形**に寄せる。

**型番と製造元は伏せる。** 汎用的な名前で表し、端子番号と挙動だけを実機に合わせる（ユーザー指示）。端子番号は本プロダクトの価値そのものなので、そこは実機どおりにする。

### 読み取れた仕様（3 機種）

#### ① 調光コントローラ（46 端子・AC100V）

| 端子 | 役割 |
|---|---|
| 1–8 | フェーダー調光信号出力 1–8（0–10V） |
| 9–16 | 照明スイッチ調光信号出力 1–8（0–10V） |
| 17–20 | 未接続（No Connect） |
| 21 | GND |
| 22 / 23 | 通信線 ＋ / −（RS485） |
| 24–31 | 照明スイッチ 1–8 ON/OFF 出力（オープンコレクタ） |
| 32 | 電源 ON/OFF 出力（オープンコレクタ） |
| 33 | 補助ランプ ON/OFF 出力（オープンコレクタ） |
| 34 / 35 / 36 | 昇降 上昇 / 停止 / 下降 出力（オープンコレクタ） |
| 37 | AUX2 ON/OFF 出力（オープンコレクタ） |
| 38 / 39 | 電源制御 ノーマルオープン / ノーマルクローズ（オープンコレクタ） |
| 40 / 41 | フライホイールダイオード |
| 42 / 43 | フォトカプラ入力 ＋（DC12–24V）/ −（0V） |
| 44 / 45 / 46 | GND |

- 調光出力：**0–10V が 16 回路**（20mA/回路）、1kHz PWM が 2 回路（50mA/回路）
- ON/OFF 出力：500mA が 16 回路
- **調光電圧の調整は「消灯時 10V、点灯時 0V」** —— 仕様書に明記されている逆特性。Step 20 の前提がここで裏付けられた

#### ② 調光操作卓（15 端子・AC100V）

| 端子 | 役割 |
|---|---|
| 1 | ＋12V 出力（100mA） |
| 2 | AUX1 オープンコレクタ出力（500mA）／電源 ノーマルオープン |
| 3 | AUX2 オープンコレクタ出力（500mA）／電源 ノーマルクローズ |
| 4 / 5 / 6 | 電源 ノーマルクローズ / コモン / ノーマルオープン（**無電圧接点**） |
| 7 / 8 | 通信線 ＋ / − |
| 9 / 12 | GND |
| 10 / 11 | フォトカプラ入力 ＋ / − |
| 13 | FG |
| 14 / 15 | AC100V（L）/（N） |

- 8 フェーダー・8 シーン記憶・照明スイッチ・昇降操作を持つ操作卓
- **4–5–6 は無電圧接点**（c 接点そのもの）

#### ③ ライトコントローラ（4 回路・DC24V）

| 端子群 | 役割 |
|---|---|
| `+1-` `+2-` `+3-` `+4-` | PWM 出力 4 回路（± 対） |
| `24V` / `GND` | 電源 |
| INPUT `1` `2` `3` `4` `G` | 調光信号入力 0–10V（4 回路＋コモン） |
| CUT RELAY `1` `2` `3` `4` `G` | カットリレー出力（4 回路＋コモン） |
| CUT ADJ. `1` `2` `3` `4` | カット動作点の調整 |

- 入力 0–10V → 出力 10V PWM
- **カットリレーは 0〜50% で動作（調整可）** —— アナログ量が閾値を跨ぐと接点が動く
- DIP：1）1kHz / 300Hz　2）**0V-ON / 10V-ON（極性切替）**　3・4）未使用

#### ④ 位相制御調光器（AC100V・800W）

| 端子 | 役割 |
|---|---|
| IN / COM / OUT | AC100V 入力 / コモン / AC 調光出力 |
| CN / GND / OFF | 調光信号（0–10V）/ 信号コモン / **強制出力遮断** |

- 調光信号 DC 0–10V（0–100%）、入力抵抗 22kΩ
- **DIRECT スイッチで完全直点灯**
- **OFF 入力を GND に接続で出力遮断**
- **調光信号極性は DIP で反転可**
- 調光カーブ切替：リニヤー／2 乗特性
- 調光下限：可変抵抗で 0〜50%、調光上限：DIP で 100/90/80/70%
- MOSFET による逆位相制御、電源周波数の自動判別

## 今回のスコープ

**Step 21 — 調光の入出力を「機器」として置けるようにする。**

Step 20 の最小 2 部品を、実機の形に寄せる。**アナログと接点の橋渡し（カットリレー・オープンコレクタ・無電圧接点）は次スコープ。**

### 含むもの

- **多チャンネルのアナログ出力** —— 今の `analog-source` は信号端子 1 本。16 回路を 1 部品で持てるようにする
- **ランプ以外の機器がアナログ入力を持てるようにする** —— 今の `DimmingInput` は `kind: "lamp"` にしか付かない
- **位相制御調光器**（AC 入力 → 調光された AC 出力。0–10V 入力・DIRECT・強制遮断）
- **調光コントローラ**（0–10V 16 回路の出力。オープンコレクタ出力は端子として出すが、次スコープまで接点としては働かない）
- **極性・上限・下限をインスタンスごとに持つ** —— 実機の DIP と可変抵抗にあたる。**定義に固定しない**（タイマーの `presetMs`・ランプの `lampColor` と同じ考え方）
- 調光カーブの形（リニヤー / 2 乗特性）

### 含まないもの

- **カットリレー**（アナログ → 接点）—— 次スコープ
- **オープンコレクタ出力・無電圧接点**（接点 → GND）—— 次スコープ。端子は出すが働かない
- **調光操作卓** —— 上記 2 つが要るので次スコープ
- **通信（RS485）** —— 端子は出すが、電位が届くモデルでは意味が出ない。逆結線の静的チェックは将来
- **フォトカプラ入力**・フライホイールダイオード端子
- **PWM の波形**・位相制御の波形・周波数（1kHz / 300Hz）・力率・突入電流
- **シーン記憶・フェーダー操作・パラメータ設定** —— 操作卓の内部ロジック
- 定格容量（800W / 20mA / 500mA）の超過判定

### 端子データの確度

**社内仕様書を一次資料とする。** 型番と製造元は伏せるが、`source` には**資料の版と日付**を残す —— 伏せたせいで後から再検証できなくなるのを防ぐ（CLAUDE.md 設計原則 5 の趣旨）。実機の仕様書を読んで書き写したので `verified: true`。

## ユーザーストーリーと受け入れ条件

### US-AO 調光コントローラを置いて 16 回路を配れる

- 0–10V 出力が 16 回路ぶん、実端子番号（1–16）で出る
- **回路ごとに違う電圧を出せる**（フェーダー 1 を 0V、フェーダー 2 を 10V など）
- GND（21・44・45・46）が信号のコモンとして働く
- 未接続（17–20）の端子が「使わない端子」として警告に埋もれない

### US-AP 位相制御調光器で AC の負荷を調光できる

- AC100V を IN / COM に入れ、OUT に調光された出力が出る
- CN に 0–10V を入れると、その電圧で OUT の明るさが決まる
- **OFF を GND に繋ぐと出力が遮断される**（明るさ 0%）
- **DIRECT にすると信号によらず 100%** になる
- 調光信号が未接続なら、定義が持つ未接続時レベルとして扱われる

### US-AQ 極性と上下限を実機どおりに設定できる

- プロパティパネルで**極性の反転**を切り替えられ、0V=100% と 10V=100% を選べる
- **調光上限**（100 / 90 / 80 / 70%）と**調光下限**（0〜50%）を設定できる
- **調光カーブ**（リニヤー / 2 乗特性）を選べる
- どれもインスタンスごとに持ち、保存・読み込み・Undo が効く
- **エンジンに型番分岐が増えない**（設定は定義とインスタンスから読む）

## 完了判定

- `npm test` が緑
- US-AO〜AQ が動く
- `design.md` §3・§4・§5 と `requirements_definition.md` §6.1 が更新されている

---

## 引き継ぎメモ（リポジトリの外の決定事項）

- **実機の仕様書は社内資料。型番・製造元・製品名はリポジトリに書かない**（ユーザー指示）。端子番号と挙動だけを実機に合わせる
- 調光の逆特性（0V=100%）は仕様書に明記あり。ただし**実機側は極性切替式**なので、エンジンではなくインスタンス設定として持つ
- ログイン：Supabase Auth + RLS で確定。未決は「公開プロダクトのまま続けるか社内ツールに閉じるか」の 1 点
- ブランチは `claude/dimming-system-simulation-i8xn8y`。`dev_v0.1` 経由で `main` へ（`main` への push = 本番デプロイ）
