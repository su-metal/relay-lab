# 作業単位の要求 — 電磁接触器と AC 電源

> このファイルは作業単位ごとに上書き更新される。プロダクト全体の要件は `requirements_definition.md`、技術設計は `design.md` を参照。
>
> 前の作業単位（Step 0〜18）は完了済み。部品を配置し、実端子番号どうしを配線し、▶ で動作を確認でき、保存・書き出し・Undo・診断・部品検索・範囲選択・部品交換・操作ヘルプ・停止中の配線チェック・電流の向き・励磁経路の言語化・接点の図記号・タイマー（限時）・モバイル表示・部品の整列・経路確認モード・配線の部品回避・表示ランプの色・ラダー図への変換まで揃っている。

## 今回のスコープ

**Step 19 — 電磁接触器と AC 電源を置けるようにする。**

調光盤（実機の制御盤）を再現する足がかり。盤の負荷側は DC24V の制御回路ではなく、**AC100V を電磁接触器で入り切りする**構成になっている。ここが置けないと、制御回路だけを描いて「その先が図に無い」状態になる。

いずれも**実型番を持たない汎用部品として始める**。実型番（富士電機 SC / 三菱 S-T / OMRON J7KN など）は公式カタログの図を確認できたときに足す —— タイマーを汎用から始めたのと同じ進め方（CLAUDE.md 設計原則 5）。

### ① b 接点だけの接点が型で表せない

電磁接触器の補助接点は **a 接点 1 個・b 接点 1 個**（1a1b）が基本構成。ところが `RelayContact.noTerminal` が必須なので、**「NO 端子が実機に無い接点」を書く場所が無い。**

G7L で `ncTerminal` を省略可能にして a 接点のみを表せるようにしたのと、**左右対称の穴**。エンジン側は `closedContactPairs()` / `openContactPairs()` がすでに両側とも `undefined` を同じ形で弾いているので、**型を 1 段広げるだけでエンジンの差分は 0 行**になるはず。ここが崩れたらデータ駆動設計が破綻しているサイン（Step 7 と同じ検証を兼ねる）。

### ② AC 電源が無い

`ElectricalDefinition` の `power` は `currentType: "DC" | "AC"` をすでに持ち、**エンジンは `currentType` を一度も読んでいない**（`src/circuit/engine/` に 1 度も出現しない）。つまり AC 電源は定義 1 枚で足りる。

足りないのは端子の役割だけ。`TerminalRole` に L / N を表す値が無く、`power_positive` / `power_zero` を当てると **AC に + と 0V があるように読める。** G7L で極性なしコイル用に `coil` を足したのと同じ 1 語の追加で済む。

### 含むもの

- `RelayContact.noTerminal` を省略可能にし、接点の形に `SPST-NC` を足す
- **汎用電磁接触器**（主接点 3 極 ＋ 補助 1a1b ＋ AC100V コイル）
- **AC100V 電源**（L / N）と `TerminalRole` の `power_line` / `power_neutral`
- 接点構成の表示（`contactSummaryOf`）を a / b / c の混在に対応させる
- b 接点のみの接点の図記号・プロパティパネル表示

### 含まないもの

- **ブレーカー・サーマルリレー** —— 3 極が連動する 1 つのスイッチを表すには `kind: "switch"`（端子 2 個・状態 2 つ固定）の一般化が要る。別スコープ
- **実型番の電磁接触器** —— 公式カタログの図と照合する工程が要る
- **アナログ調光（0–10V）** —— 電圧値を持つ設計変更そのもの。別スコープ
- **三相回路・接地（FG）** —— エンジンに相と大地の概念が無い
- **定格電圧の不一致検出** —— AC100V の負荷を DC24V 電源に繋いでも警告は出ない（`design.md` §6-3 のまま）
- **AC の交流としての扱い** —— 極性・位相・実効値は扱わない。エンジンは今までどおり「同じ 1 台の電源の + 側と 0V 側に届くか」だけを見る

## ユーザーストーリーと受け入れ条件

### US-AH 電磁接触器を置いて配線できる

**制御盤を図にするユーザーとして、** 制御回路の先にある負荷の入り切りまで 1 枚に描きたい。

- パレットから電磁接触器を置ける
- 主接点 3 極（`1/L1`–`2/T1`・`3/L2`–`4/T2`・`5/L3`–`6/T3`）、補助 a 接点（`13`–`14`）、補助 b 接点（`21`–`22`）、コイル（`A1`・`A2`）の **12 端子**が出る
- コイルが励磁すると主接点 3 極と補助 a 接点が閉じ、補助 b 接点が開く
- コイルに極性が無い（AC コイル）ので、どちら向きに繋いでも励磁する
- 本体に「4a1b ／ コイル AC」と出る（主接点 3 極＋補助 a を a 接点、補助 b を b 接点として数える。"5c" と丸めない）

### US-AI b 接点だけの補助接点が正しく振る舞う

**インターロックを組むユーザーとして、** 相手の接触器の b 接点で自分を止めたい。

- 非励磁のとき `21`–`22` が閉じ、励磁すると開く
- **`21`–`22` に「COM–NO」や存在しない NO 端子の行が出ない**（G7L で b 接点の行を出さないのと対称）
- 図記号でも、届かせる先の無い可動片が NC から離れる向きに描かれる
- 2 台の接触器を互いの b 接点で縛ると、先に入れた側だけが励磁して止まる

### US-AJ AC100V 電源で回路を組める

**盤を図にするユーザーとして、** 制御は DC24V、負荷は AC100V という実際の構成をそのまま描きたい。

- パレットから AC100V 電源を置け、端子が `L` / `N` と出る
- 端子の役割が「電源 L（非接地側）」「電源 N（接地側）」と出て、**`+` や `0V` と書かれない**
- L と N を直結すれば電源短絡の警告が出る（DC と同じ判定が効く）
- **DC24V 電源と AC100V 電源を混在させたとき、片方の L ともう片方の 0V をまたいだ負荷は通電しない**（既存の `supplyMismatch` がそのまま効く）

## 完了判定

- `npm test` が緑
- **`src/circuit/engine/` の差分が 0 行**（Step 7・Step 17 と同じデータ駆動の検証）
- `design.md` §3・§4・§9 と `requirements_definition.md` §6.1 が更新されている
