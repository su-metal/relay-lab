# 作業単位の要求 — MVP 第一目標

> このファイルは作業単位ごとに上書き更新される。プロダクト全体の要件は `requirements_definition.md`、技術設計は `design.md` を参照。

## 今回のスコープ

`design.md` §9 の **Step 0 〜 Step 4**。

「部品を配置し、実端子番号どうしを配線し、▶ シミュレーションを実行すると押しボタン操作でリレーが励磁し、接点が切り替わってランプが点灯する」までを画面上で成立させる。

### 含むもの

- Next.js / TypeScript / React Flow / Zustand / Vitest のセットアップ
- `npm test` を Stop フックに登録する（検証ゲート）。テストが落ちている状態で応答を終了できないようにする
- 型定義一式（`ComponentDefinition` / `TerminalDefinition` / `RelayDefinition` / `CircuitDocument` / `SimulationResult`）
- 部品定義 4 種
  - DC24V 電源
  - モーメンタリ押しボタン A 接点
  - モーメンタリ押しボタン B 接点
  - OMRON MY4N DC24V
  - DC24V 表示ランプ
- シミュレーションエンジン（Union-Find によるネット構築、収束ループ、コイル励磁判定、接点切替、発振検出）
- 検証回路テスト 1〜5 の Vitest 実装
- 3 カラム画面（部品パレット / キャンバス / プロパティパネル）と操作バー
- パレットからキャンバスへのドラッグ＆ドロップ配置、部品移動、削除
- 端子どうしの配線（React Flow Handle）
- ▶ シミュレーション開始 / ■ 停止
- 押しボタンのモーメンタリ操作（マウスダウンで押下、マウスアップで復帰）
- 配線の状態別カラーリングとランプの発光表示

### 含まないもの（次以降の作業単位）

- MY2N / MY4N-D2 / 端子台 / ダイオードの部品定義（Step 7）
- 部品検索 UI（Step 7）
- プロパティパネルの詳細表示・端子ツールチップ（Step 5）
- 保存 / 読込 / Undo / Redo（Step 6）
- 警告の UI 表示（Step 6。エンジン側の検出ロジックは今回実装する）

## ユーザーストーリーと受け入れ条件

### US-A エンジンが検証回路を正しく解く

**開発者として、** UI を使わずに回路の動作を検証したい。

`src/circuit/engine/__tests__/scenarios.test.ts` で以下 5 本が緑になること。

| # | 回路 | 期待結果 |
|---|---|---|
| 1 | `+24V → S1(A接点) → RY1コイル → 0V` | S1 押下中のみ RY1 励磁 |
| 2 | `+24V → RY1のA接点 → L1 → 0V` | RY1 励磁時に L1 点灯 |
| 3 | 自己保持（S1 と RY1 の A 接点を並列） | S1 を一瞬押しただけで RY1 が励磁し続ける |
| 4 | 停止付き自己保持（S2 の B 接点を直列に追加） | S1 で自己保持、S2 押下で消磁 |
| 5 | インターロック（RY1・RY2 の B 接点を相互に直列） | 片方が励磁中はもう片方が励磁しない |

いずれも `status === "stable"` で収束すること。

### US-B 部品を配置して配線できる

**ユーザーとして、** 部品を並べて端子どうしを繋ぎたい。

- パレットからキャンバスへドラッグ＆ドロップで部品を追加できる
- 配置後に自由に移動でき、移動しても配線は切れずに追従する
- キャンバスでパン・ズーム・複数選択・削除ができる
- 端子から端子へドラッグして配線できる
- **部品本体への接続はできない**（接続は必ず端子 → 端子）
- MY4N の端子に実端子番号（1〜14）が視覚的に表示される

### US-C シミュレーションが画面上で動く

**ユーザーとして、** 組んだ回路の動作を目で確認したい。

- ▶ で開始、■ で停止できる
- 押しボタンをマウスダウンで押下、マウスアップで復帰する
- MY4N が励磁すると 4 回路すべての接点が同時に切り替わる
- ランプが条件を満たすと発光表示される
- 配線が状態に応じて色分けされる（グレー / 赤 / 青 / 緑＋発光）

### 完了判定

`requirements_definition.md` US-1〜US-3 のうち、次が画面上で通しで再現できること。

```
DC24V電源 ・ S1 ・ OMRON MY4N ・ ランプ を配置
  → 端子どうしをドラッグして配線
  → ▶ シミュレーション
  → S1 を押す
  → MY4N 励磁 → A接点切替 → ランプ点灯
```

## 制約事項

- エンジンは React / Zustand / React Flow を import しない。純粋関数として実装する
- エンジンに `if (model === "MY4N")` のような型番分岐を書かない
- 負荷（コイル・ランプ）をグラフ上で導通させない（`design.md` §5.2）
- 部品定義には `source`（出典 URL）と `verified: false` を必ず記載する。MY4N の端子番号は Web 調査による仮置きであり、実機／公式データシートでの検証待ち
- 収束計算の最大反復回数は 100。超過時は `not-converged`、状態の再出現時は `oscillating` を返す
- 記述・UI とも日本語
