# 作業単位の要求 — 初見で使えるようにする（ヘルプ・配線チェック・CI）

> このファイルは作業単位ごとに上書き更新される。プロダクト全体の要件は `requirements_definition.md`、技術設計は `design.md` を参照。
>
> 前の作業単位（Step 0〜9）は完了済み。部品を配置し、実端子番号どうしを配線し、▶ で動作を確認でき、保存・書き出し・Undo・診断・部品検索・範囲選択・部品交換まで画面上で動く。機能としての MVP は満たしている。

## 今回のスコープ

**Step 10 — 機能ではなく、機能に辿り着けない部分を埋める。**

回路モデルは揃ったが、**画面はまだ「知っている人にしか使えない」。** D で削除、F で反転、L で整列、画面移動は Shift+ドラッグ —— どれも画面のどこにも書かれていない。診断も ▶ を押すまで 1 件も出ないので、「実機を配線する前に確認する」という目的に対して一手遅い。そして push した変更を検証するものが手元の Stop フックしか無い。

### 含むもの

- **操作ヘルプ。** 操作バーの `?` から、基本の 3 手・操作一覧・`design.md` §6 の「扱わないこと」を 1 枚で出す
- **キー割り当ての単一の出典。** React Flow へ渡す値もヘルプの表も `lib/shortcuts.ts` の定数から作る
- **停止中の配線チェック。** 静止状態で分かる指摘（未接続の端子・電源短絡・ダイオードの向き）を ▶ の前に出す
- **CI。** push / PR で型検査・テスト・本番ビルドを回す

### 含まないもの

- 回路を複数枚保存する（LocalStorage は 1 スロットのまま）
- 直列負荷の検出、電圧・電流の数値計算、タイマー（`design.md` §6）
- 部品の追加（MY2N-D2 等）
- ESLint の導入

## ユーザーストーリーと受け入れ条件

### US-I 操作を推測しなくていい

**初めて開いたユーザーとして、** どう操作するのかを画面から知りたい。

- 操作バーの `?` でヘルプが開き、Esc・× ・背景クリックで閉じる
- **画面移動が Shift+ドラッグであることが書いてある**（素の左ドラッグは範囲選択）
- D / F / L と Undo / Redo が一覧になっている
- 「直列につないだランプは両方消灯する」など、仕様上そうなる挙動が書いてある
- **ヘルプを開いたまま D や L を押しても背後の回路が変わらない**
- ヘルプの表と実際のキー割り当てがずれたらテストが落ちる

### US-J 電源を入れる前に配線ミスに気づける

**配線を確かめたいユーザーとして、** ▶ を押す前に分かる誤りは先に知りたい。

- 停止中の右下パネルが「配線チェック」になり、未接続の端子・静止状態の電源短絡・還流ダイオードの逆挿しが出る
- **B 接点越しの直結は静止状態で閉じているので出る。** A 接点越し（押して初めて閉じる）の短絡は出ない
- **停止中に指摘が無いことを「正しい」と読ませない。** 見ている範囲を画面に明示する
- 部品が 1 個も無いときに「指摘はありません」と言わない
- ▶ を押すと従来どおりの「診断」に戻り、同じ指摘が二重に出ない

### US-K push した変更が検証される

**開発者として、** 手元のフックを通らない変更を止めたい。

- push / PR で `npm run typecheck` → `npm test` → `npm run build` が走る
- 落ちた理由が型・テスト・ビルドのどれなのかがステップで分かれている

### 完了判定

```
? を開く → 操作一覧と制約が読める → 開いたまま D を押しても部品が消えない → Esc で閉じる
  → ダイオードを逆挿しした回路を停止中に見る → 「配線チェック」に指摘が出る
  → ▶ を押す → 「診断」に切り替わり、同じ指摘が二重に出ない
  → npm test / npm run typecheck / npm run build がすべて通る
```

## 制約事項

- **静的チェックはエンジン側の純粋関数（`circuit/engine/wiring.ts`）に閉じる。** React コンポーネントに判定を書かない（CLAUDE.md 設計原則 1）
- **`engine/wiring.ts` に型番分岐を書かない。** 既存の検出関数（`validation.ts`）を静止状態のネットで呼ぶだけに留める
- **キー割り当ての定義を 2 箇所に持たない。** ヘルプは定数から組み立て、テストで一致を押さえる
- ヘルプの開閉は画面の操作モードであり、`circuitStore`（保存対象＋履歴）に混ぜない
- 記述・UI とも日本語
