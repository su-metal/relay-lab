# 作業単位の要求 — 調光（アナログ量）を扱えるようにする

> このファイルは作業単位ごとに上書き更新される。プロダクト全体の要件は `requirements_definition.md`、技術設計は `design.md` を参照。
>
> 前の作業単位（Step 0〜19）は完了済み。部品を配置し、実端子番号どうしを配線し、▶ で動作を確認でき、保存・書き出し・Undo・診断・部品検索・範囲選択・部品交換・操作ヘルプ・停止中の配線チェック・電流の向き・励磁経路の言語化・接点の図記号・タイマー（限時）・モバイル表示・部品の整列・経路確認モード・配線の部品回避・表示ランプの色・ラダー図への変換・電磁接触器と AC 電源まで揃っている。

## 背景 — なぜこれをやるのか

実機の調光盤（制御盤の写真から）を再現するのが目的。盤は 3 層でできている。

| 層 | 状態 |
|---|---|
| 接点で組んだ制御回路（リレー・端子台） | **対応済み**（Step 0〜17） |
| AC100V を電磁接触器で入り切りする負荷側 | **対応済み**（Step 19） |
| **0–10V のアナログ量で明るさを決める調光** | **これから（本スコープ）** |

ここが入らないと、盤の中心にある「調光」そのものが図に描けない。

## 今回のスコープ

**Step 20 — 電圧値を持つアナログ量を導入し、0–10V の調光を再現する。**

### 決まっていること

**① ユーザーの会社の調光仕様は逆特性。0V = 100%、10V = 0%。**

一般的な 0–10V 機器（0V = 消灯）と真逆。この 1 点が以下すべての判断に効く。

**② 端子が持つのは電圧（V）。% への変換は部品側。**

`if (model === "FMD-701D") invert` は書けない（CLAUDE.md 設計原則 2）。アナログのネットが持つのは電圧値だけにして、`0V → 100%` / `10V → 0%` という対応を**定義側に宣言で持たせる**。順特性の機器を後から足してもエンジンは 1 行も変わらない。

**③ 断線＝全灯。これが本スコープ最大の価値。**

0V = 100% なので、**調光信号線を挿し忘れる／抜けると全灯する。** 一般的な機器と真逆で、しかも「消えている」より気付きにくい。`engine/validation.ts` はすでに全部品の全端子を舐めて `unconnected-terminal` を出しているので、そこに「この端子が未接続なら出力は 100% になります」と言わせる。

**ただし未接続時に何 V になるかはエンジンが決め打ちしない。** 実機の入力回路（プルアップ／プルダウン）次第なので、**定義側に「未接続時のレベル」を持たせる。**

**④ 配線色の規則から外す。**

`design.md` §5.6 の `WireState` は「電位が届いているか」で色を決める。0V の調光信号線は `zero`（青）か `inactive`（グレー・不透明度 0.45）になり、**100% 全灯している線が「非通電」に見える。** CLAUDE.md 設計原則 8（タイマー計測中のコイル配線が灰色に見える取り違え）と同じ種類の事故。アナログ線は導通の配色から外し、レベルの濃淡なり専用色なりで別に描く。

**⑤ 電源短絡の判定対象から外す。**

調光出力を 0V に落とすのは、この仕様では**正常な操作（全灯）**。`power-short-circuit` を機械的に当てると、正しい配線が最も危険な警告で真っ赤になる。

**⑥ 接点で信号線を 0V に落とせる（DIRECT 相当）。**

盤の "DIRECT" は、リレー接点で調光信号線を 0V コモンに繋いで全灯させる配線と読める。**これは既存の Union-Find でそのまま表せる** —— アナログのために別のグラフを作る必要はない。「調光器が出しているレベル」対「0V への強制」は、**外部から 0V に落とされたら 0V 優先**という単純な規則で実務に合う（出力段のインピーダンスはモデル化しない）。

**⑦ GND を共有していない調光信号は成立しない。**

0–10V は 0V を基準にした電圧なので、リターンが共通でないと意味を持たない。既存の `supplyMismatch`（§5.3）とまったく同じ話で、同じ考え方がそのまま効く。「調光信号だけ引いて GND を共通にしていない」を捕まえられる。

### 含むもの

- アナログ量を持つネット（導通レイヤは触らず、**第 2 パスとして重ねる** —— ダイオードが DSU を変えずに §5.4 で電位を一方向に流しているのと同じ場所・同じ手口）
- **汎用 0–10V 調光出力**と**汎用調光ランプ**（実型番なし。変換カーブを定義側に宣言で持たせる）
- 未接続時のレベルを定義に持たせ、`unconnected-terminal` に「未接続なら 100%」を言わせる
- アナログ線を配線色の規則から外す／電源短絡の判定から外す
- 接点で信号線を 0V に落とすと全灯する（DIRECT 相当）
- 明るさ（%）の表示。端子には V、部品には % を出す

### 含まないもの

- **FMD-701D そのものの定義** —— 次スコープ（下記の未決事項が要る）
- **フェード（時間をかけて明るさが変わる）** —— 既存のタイマー基盤（`nowMs` を入力で受ける・実行時状態を返して次回入力に繋ぐ・`nextEventAtMs` で再計算の要否を決める）がそのまま土台になるが、本スコープでは即座に変わるものとして扱う
- **通信（RS-485 / DMX）** —— 端子グラフには乗るが「電位が届く」モデルでは意味が出ない。A/B 逆結線や終端抵抗の静的チェック（`engine/wiring.ts` の枠）なら安いが、本スコープ外
- 位相制御（トライアック）・力率・消費電力の数値
- 三相・接地（FG）

## 着手前に確定が要ること（次スコープ用・本スコープは進められる）

**アナログレイヤ自体は汎用部品で閉じるので、以下が無くても本スコープは完了できる。** FMD-701D の定義（次スコープ）にだけ必要。

1. **接点入力（SW1… 端子）が何をするか** —— どの入力でどのチャンネルがどのレベルになるか
2. **信号線が未接続のときのレベル** —— 実機の入力回路がプルアップかプルダウンか
3. **DIRECT / CUT が信号線に何をするか** —— 0V に落とすのか、別系統に切り替えるのか

## ユーザーストーリーと受け入れ条件

### US-AK 調光器とランプを繋いで明るさが出る

**盤を図にするユーザーとして、** 0–10V で明るさが決まる様子をそのまま描きたい。

- パレットから調光出力と調光ランプを置け、繋ぐと明るさが出る
- **出力 0V でランプが 100%、10V で 0%**（会社の仕様どおり）
- 端子のツールチップには **V**、部品には **%** が出る
- 明るさが 0% と 100% の中間でも、絵で読める

### US-AL 挿し忘れが全灯として警告される

**配線を確認するユーザーとして、** 気付きにくい失敗を先に教わりたい。

- 調光信号線が未接続だと、停止中の診断に「未接続。出力は 100% になります」と出る
- 未接続時のレベルは定義から来る（エンジンが決め打ちしない）
- **GND を共通にしていない調光信号は成立しない**と言い分けられる（接点の話をしない）

### US-AM 接点で全灯にできる（DIRECT）

**盤を再現するユーザーとして、** リレー接点で調光をバイパスしたい。

- リレー接点で調光信号線を 0V コモンに繋ぐと、接点が閉じている間 100% になる
- 接点が開くと調光器の出したレベルに戻る
- **この配線が電源短絡として警告されない**

### US-AN アナログ線が「非通電」に見えない

**図面を読むユーザーとして、** 効いている線を効いていないと読み違えたくない。

- 0V を出している調光信号線が、グレー（非通電）で描かれない
- レベルの違いが線から読める
- 導通の配線（赤・青・緑）とアナログ線が見分けられる

## 完了判定

- `npm test` が緑
- 上記 US-AK〜AN が動く
- `design.md` §3・§4・§5・§6 と `requirements_definition.md` §6.1・§8 が更新されている
- **`design.md` §6-3（電圧・電流・消費電力の数値は扱わない）と `src/lib/help-content.ts` の `LIMITATIONS` が同じ作業の中で書き換わっている** —— この 2 つは同じ内容の写しで、片方だけ直すと画面とドキュメントが食い違う

---

## 引き継ぎメモ（リポジトリの外の決定事項）

コードにもドキュメントにも書けないが、次の判断に要るもの。

### FMD-701D について

- **社内オリジナル製品。** 公開データシートは存在しない
- **リポジトリ・公開サイトに載せてよい**（ユーザー確認済み）
- **調光仕様は 0V = 100% / 10V = 0%**（一般的な 0–10V と逆）
- 実端子記号を主張するときの一次資料は**社内図面**になる。`source` に図面番号・版数・確認日を残さないと後から再検証できない（CLAUDE.md 設計原則 5 の趣旨。社内製品は改訂されるので外部品より重要）

### ログインについて

- **目的は「社内部品を見せる相手を絞ること」だけ**（ユーザー選択済み）
- **方式は Supabase Auth + RLS で確定。** 静的書き出し（`output: "export"`）のままログインを付けられ、`wrangler.jsonc` の前提を壊さない
- **クライアント側ログインだけでは守れない。** 静的 SPA なので、定義を TypeScript に書けば JS チャンクで全訪問者に配られる。守るには定義をサーバ側（Supabase）に置いて RLS を張るしかない
- **未決の 1 点** —— relay-lab を公開プロダクトのまま続けるか、社内ツールに閉じるか。社内ツールでよいなら **Cloudflare Access が実装ゼロ**で済む（ただしサイト全体が社内限定になり、`requirements_definition.md` §2 の読者に届かなくなる）
- 着手時は `CLAUDE.md`（バックエンド・ログイン・DB は持たない）と `requirements_definition.md` §7・§8 を同じ作業の中で書き換える
- エンジンへの影響は無い。`simulate(doc, defs, input)` は定義レジストリを引数で受け取り、`ComponentDefinitionRegistry` はただの `ReadonlyMap` なので、取得元が増えてもエンジンもテストも無傷

### ブランチとデプロイ

- 作業は **`dev_v0.1`**。`main` へは PR で上げる（`main` への push = 本番デプロイ）
- 現在 `dev_v0.1` と `main` は一致（`7d481f6`）
- **CI 経由のデプロイはまだ一度も成功していない。** `CLOUDFLARE_API_TOKEN` を Actions Secrets に登録済みだが、run #11 は attempt 1 のまま failure で、再実行はされていない。本番へは手元から配った（`npm run deploy`）。**次に `main` へ push したときが、CI 経路の初めての検証になる**
