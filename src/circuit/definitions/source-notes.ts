/**
 * 端子データの出典に付ける定型文。
 *
 * 実型番の部品はデータシート等の URL を `source` に書く（定義ファイル 1 枚で完結させる）。
 * 実型番を持たない汎用部品だけがこの定型文を共有する。
 */

/**
 * 電源・押しボタン・ランプなど、実型番を持たない部品用。
 *
 * これらの端子ラベルは実端子番号ではない。MY4N の 13 / 14 のような
 * 実端子番号と同列に扱わないことを、出典の文面で明示しておく。
 */
export const GENERIC_TERMINAL_SOURCE =
  "汎用部品（実型番なし）。端子の呼称は本アプリの便宜的なもので、実端子番号ではない";
