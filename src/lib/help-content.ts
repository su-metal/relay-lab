/**
 * ヘルプの本文（design.md §8.10）。
 *
 * **`LIMITATIONS` は design.md §6「既知の制約」を画面に出したもの。**
 * §6 は「これらは UI で明示し、ユーザーが誤解しないようにする」と決めており、
 * ここがその実体になる。制約を黙っていると、シミュレーターの仕様上そうなる挙動
 * （直列につないだランプが両方消灯する等）がバグに見える。
 *
 * §6 を直したらこのファイルも直す。逆も同じ。
 */

/** 最初の 3 手。部品を置く → 繋ぐ → 動かす */
export const BASIC_STEPS: readonly string[] = [
  "左のパレットから部品をキャンバスへドラッグして置く。",
  "部品のふちにある小さな丸（端子）から、繋ぎたい相手の端子へドラッグする。",
  "操作バーの ▶ で解析を始め、押しボタンを押して動作を確かめる。停止すると押下状態と励磁状態は消える。",
];

export type Limitation = {
  title: string;
  body: string;
};

/**
 * このシミュレーターが**あえて扱わないこと**（design.md §6）。
 *
 * 並びは「実際に踏みやすい順」。先頭 2 つは初学者が必ず一度は組む配線で、
 * 残りは「なぜこの機能が無いのか」に答えるためのもの。
 */
export const LIMITATIONS: readonly Limitation[] = [
  {
    title: "ランプを直列につなぐと両方とも消灯する",
    body: "＋24V → ランプ → ランプ → 0V のようにランプを直列にすると、実機では両方が薄暗く点灯しますが、本アプリでは両方消灯として扱います。電圧を計算せず「＋側と 0V 側に届いているか」だけで判定しているためです。なお、コイルを直列にした場合の「どちらも動作しない」は実機と同じ結果になります（24V を 2 個で分け合っても吸引電圧に届かないため）。",
  },
  {
    title: "電圧・電流・消費電力の数値は扱わない（調光の 0–10V を除く）",
    body: "電源まわりは導通の有無だけを判定します。定格電圧の不一致（DC24V のランプに別電圧をかける等）や、ダイオードの順電圧降下（約 0.7V）は再現しません。電圧値を持つのは 0–10V の調光信号だけで、これは導通とは別の量として重ねてあります —— 調光信号線を電源に繋いでも電圧は伝わりませんし、逆に調光信号を 0V に落としても電源短絡にはなりません。",
  },
  {
    title: "調光は 0–10V の値だけ。フェードも通信も扱わない",
    body: "調光信号は 0–10V の電圧と、そこから決まる明るさ（%）だけを扱います。時間をかけて明るさが変わるフェード、RS-485 や DMX による調光、位相制御（トライアック）は扱いません。V から % への対応は機器ごとの設定で、この盤では 0V＝100%・10V＝0% の逆特性です —— 調光信号線を挿し忘れると消えるのではなく全灯するので、停止中の配線チェックでそう指摘します。",
  },
  {
    title: "扱う時間はタイマーの限時だけ",
    body: "オンディレイ（限時動作）とオフディレイ（限時復帰）は再現します。それ以外の時間 —— リレーの動作時間・復帰時間、接点が切り替わる途中の過渡、タイマーのフリッカ（周期）動作やワンショット —— は扱いません。発振する回路は「発振します」と示すだけで、周期は再現しません。",
  },
  {
    title: "チャタリングは「起きる配線か」だけを判定する",
    body: "コイルが自分自身の b 接点を通して給電されていると、動作した瞬間に自分で給電を切るため実機では唸ります。この配線は「チャタリング」として警告しますが、唸っている様子そのものは再現しません —— ランプは点いたまま、状態も安定と表示されます。実機の c 接点は切り替わる途中にどちらにも繋がらない一瞬があり、その過渡を状態として持っていないためです。警告が出た回路は、画面の表示が正しく見えても実機では唸ると読んでください。",
  },
  {
    title: "逆起電力のサージそのものは再現しない",
    body: "ダイオードは向きによる導通の別と、還流ダイオードの向きの正誤までを判定します。消磁の瞬間に出るサージ電圧は時間の概念が無いため扱いません。",
  },
  {
    title: "同時に押した 2 つのボタンの競合は解けない",
    body: "すべてのコイルを一斉に評価するため、相互 B 接点のインターロック回路で 2 つの起動ボタンを同時に押すと、実機のように「わずかに早い方が勝つ」のではなく発振として扱います。動作時間を持たない以上、どちらが勝つかを決める根拠がありません。",
  },
  {
    title: "ラダー図は出せない配線をそのまま出せないと言う",
    body: "配線からラダー図を組み立てますが、直列と並列だけでは表せない配線（ブリッジ状に繋がった回路）と、母線に届いていない配線は段にできないため、理由を出して諦めます。ダイオードは図に出しません —— 還流ダイオードはコイルと並列に入る実装上の部品で、ラダー図の論理には現れないためです。逆向きの変換（ラダー図を描くと配線ができる）は扱いません。",
  },
  {
    title: "範囲選択は端子どうしを結ぶ直線で当たりを取る",
    body: "画面上の配線は折れ線で描かれますが、枠に触れたかどうかは両端の端子を結ぶ直線で判定します。大きく回り込んだ配線では見た目とずれることがあります。",
  },
];
